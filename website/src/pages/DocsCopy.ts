import type { Language } from '../content'
import type { DocsContent } from '../content'

/*
 * The Docs page's copy. It lives beside the page rather than in content.ts so the
 * route can be loaded on demand: this text is 114 kB and nobody
 * reads it on arrival.
 */
const docs: Record<Language, DocsContent> = {
  zh: {
    title: '文档',
    subtitle: '按使用手册的章节顺序，说明 MoonSprite 每个功能的用途、入口与操作方式。',
    outline: [
      { kind: 'page', id: 'intro' },
      { kind: 'page', id: 'quickstart' },
      { kind: 'group', id: 'g-part1', title: '创作基础', children: ['start', 'canvas', 'tools', 'colors', 'selection'] },
      { kind: 'group', id: 'g-part2', title: '结构与动画', children: ['layers', 'animation'] },
      { kind: 'group', id: 'g-part3', title: '瓦片与图像', children: ['tiles', 'image', 'slices'] },
      { kind: 'group', id: 'g-part4', title: '交付与工作区', children: ['files', 'workspace', 'prefs'] },
      { kind: 'group', id: 'g-part5', title: '扩展与参考', children: ['extensions', 'scripting', 'mse-api', 'compatibility-api', 'shortcuts'] },
      { kind: 'page', id: 'support' },
    ],
    sections: [
      {
        id: 'intro',
        title: '认识 MoonSprite',
        blocks: [
          { kind: 'p', text: 'MoonSprite 是面向 Windows 的原创像素画工作台，把绘制、逐帧动画、瓦片工作流、脚本扩展和成品导出放进同一个工作空间。它使用 Tauri 2、React、TypeScript、Zustand 与 Canvas 构建，原生运行在 Windows 10 / 11 上，全程不需要联网。' },
          { kind: 'h3', id: 'intro-features', text: '特色功能' },
          { kind: 'p', text: '下面这些工作流是 MoonSprite 投入最多、也最值得先了解的部分。逐帧动画、瓦片地图、洋葱皮、图案笔刷、分屏与脚本同样完整，只是并非这里独有的能力。' },
          { kind: 'ul', items: ['自由瓦片：把可复用图案作为可重叠、任意位置的实例摆放，改一次源图，全部引用同步更新', '蒙版：用黑白灰控制显示范围，保留原像素，并且每一帧可以拥有自己的蒙版', '图层样式：描边、阴影、内发光、颜色与渐变叠加随内容实时变化，原像素不必重画', '可编辑文本：保存文字内容、字体和排版，之后仍可回到文本层修改，固定文本框自动换行', '自动补间：按终点位移、旋转、缩放和透明度一次生成过渡帧，无需逐帧重画', '关联图层：多个图层共享同一份像素画稿，同时各自保留位置、显隐与显示效果', '缩时记录：自动保留绘画过程，可导出 MP4、WebM 或图片序列，也可随工程一起带走', '液化与 ISO 辅助：液化的推动、膨胀、收缩与扭转整理轮廓；ISO 视角把线条约束到等距阶梯方向'] },
          { kind: 'h3', id: 'intro-compare', text: '与 Aseprite 的对比范围' },
          { kind: 'p', text: '本页的对比只针对 Aseprite 官方稳定版的内置功能，以 2026 年 9 月 19 日核对的官方文档、路线图和发布记录为依据；第三方脚本与扩展不计入“内置”。比较的目的是说明工作流差异，不代表官方版本未来不会提供同样的能力。' },
          { kind: 'ul', items: ['逐帧蒙版与图层组蒙版：黑白灰控制显示范围、保留原像素，每帧可有不同蒙版', '可编辑文本图层：保存文字内容、字体与排版，之后仍能修改', '实时图层样式：描边、阴影、内发光和颜色效果随原图变化', '自动补间：按终点位移、旋转、缩放与透明度生成中间帧', '自由瓦片：实例可重叠、可任意位置摆放，并共享同一个源图', '跨图层关联内容：多个图层同步像素，同时保留各自的位置与显示属性'] },
          { kind: 'h3', id: 'intro-requirements', text: '系统要求与授权' },
          { kind: 'ul', items: ['Windows 10 或 Windows 11，安装 WebView2 Runtime（安装包会自动处理）', '源码可见；通过 Steam 与授权渠道分发的官方二进制按用户席位授权，个人与商业创作均可使用', '全部创作流程本地完成，无需联网', '界面提供简体中文、英语、日语、韩语、西班牙语、法语、德语、巴西葡萄牙语与俄语'] },
        ],
      },
      {
        id: 'quickstart',
        title: '5 分钟画出第一张作品',
        blocks: [
          { kind: 'p', text: '从新建到导出，最短路径只有七步。动画与瓦片可以在熟悉这套流程之后再看。' },
          { kind: 'h3', id: 'quickstart-new', text: '新建工程' },
          { kind: 'p', text: '按 Ctrl+N，输入名称与画布宽高，选择颜色模式；自由绘画先选 RGBA 即可。剪贴板里存在图片时，新建窗口会采用该图片的尺寸作为初始值。' },
          { kind: 'h3', id: 'quickstart-draw', text: '画第一笔' },
          { kind: 'ul', items: ['在颜色栏目选择前景色，按 B 切到铅笔，左键拖动绘制', '按 E 擦除；滚轮缩放，按住 Space 拖动画布', '按 Shift+N 新建图层，把线稿、颜色和装饰分开', '按 M 框选内容，拖动移动或按 Ctrl+T 变换，回车确认', '按 Ctrl+S 保存为 .moonsprite，保留图层与可编辑数据', '按 Ctrl+E 打开导出，选择 PNG、倍率与目录'] },
          { kind: 'p', text: '绘画写入的是“当前图层 × 当前帧”这一个单元格，不会自动修改所有帧——准备做动画时，在图层栏新增帧并逐帧修改即可。' },
          { kind: 'h3', id: 'quickstart-workspace', text: '认识工作区' },
          { kind: 'p', text: '中央是画布，四周是可停靠、可悬浮的栏目：颜色、调色板、图层与时间轴、预览、历史记录、瓦片集、图案笔刷。按 1–6 可以临时呼出常用栏目，点击外部或按 Esc 收回，不会打乱已有布局。' },
          { kind: 'h3', id: 'quickstart-next', text: '接下来看哪里' },
          { kind: 'ul', items: ['想先了解特色功能，看“认识 MoonSprite”', '想系统学绘制，从“工程与文件”按顺序往下读', '只想解决某个具体问题，先看“常见问题”'] },
        ],
      },
      {
        id: 'start',
        title: '工程与文件',
        blocks: [
          { kind: 'p', text: 'MoonSprite 以工程为单位组织创作，首页负责找回，文件菜单负责进出。' },
          { kind: 'h3', id: 'start-open', text: '新建与打开' },
          { kind: 'ul', items: ['“文件 > 新建”设置画布、颜色模式，以及是否记录绘画过程', '“文件 > 打开”、Ctrl+O，或从资源管理器直接拖入', '可打开的格式：.moonsprite、.ase、.aseprite、PNG、JPEG、WebP、BMP、GIF', '动画 GIF 会导入为多帧工程，保留可读取的帧顺序、时长与循环信息', '多个工程同时以标签打开，Ctrl+W 关闭当前工程；有未保存内容时会先提示'] },
          { kind: 'h3', id: 'start-home', text: '首页、画廊与自定义文件夹' },
          { kind: 'p', text: '首页提供最近、画廊和恢复等栏目。点击项目卡片打开；右键普通卡片可打开所在文件夹；Ctrl+滚轮切换列表大小。' },
          { kind: 'ul', items: ['在栏目管理中可添加自定义文件夹，直接浏览该目录第一层的工程与图片，栏目支持拖动排序', '移除自定义栏目只移除入口，不会删除文件', '画廊的“删除工程”会删除磁盘文件并需要确认，与“从最近移除”不同'] },
          { kind: 'h3', id: 'start-recovery', text: '恢复与项目回档' },
          { kind: 'ul', items: ['异常关闭后从首页“恢复”打开草稿，检查内容后正式保存；打开或关闭草稿不会立即删除恢复记录', '“首选项 > 文件与恢复”设置自动保存间隔与保留天数，默认保留 7 天，可设 1–365 天', '自动恢复数据不能代替主动保存工程', '启用工程备份后，已保存的项目可从“文件 > 项目回档”选择备份恢复，并可用历史撤销', '“文件 > 项目信息”查看尺寸、颜色模式、图层/组/帧数量、笔画与编辑次数、有效绘画时长与缩时记录数量'] },
          { kind: 'h3', id: 'start-timelapse', text: '缩时记录' },
          { kind: 'p', text: '新建时勾选“记录绘画过程”，或从“文件 > 缩时视频”设置录制。全程录制保留每次记录；智能采样则逐步精简较早的记录、保留近期变化，切换模式本身不会删除已有记录。' },
          { kind: 'ul', items: ['记录跟随已提交的内容操作，不记录停顿、缩放、平移与栏目拖动', '需要连修改过程一起录下时，启用“记录撤销步骤”', '可导出 MP4、WebM 或 PNG/JPG 序列；视频可设尺寸、画质与目标时长或倍速', '安装版录像保存在本机录像库；换电脑时请在“另存为”勾选“携带缩时录像”，普通保存不保证录像可用', '清空记录不删除画作，但不可撤销，也不会释放本机录像库已占用的磁盘空间'] },
        ],
      },
      {
        id: 'canvas',
        title: '画布与辅助',
        blocks: [
          { kind: 'p', text: '视图操作只改变你观察作品的方式，永远不会修改像素，也不会进入撤销历史。' },
          { kind: 'h3', id: 'canvas-view', text: '缩放、平移、旋转与镜像' },
          { kind: 'ul', items: ['缩放：滚轮或缩放工具 Z；拖动缩放时按 Shift 临时按倍率档位缩放', '平移：抓手 H、Space+拖动，或中键拖动', '旋转视图：旋转工具 R，或“窗口 > 旋转视图”，便于落笔', '镜像视图：“窗口 > 镜像视图”，检查比例与构图，不翻转原像素', '复位与显示：“窗口 > 复位视图”、全屏或高级模式（Ctrl+F）'] },
          { kind: 'p', text: '真正改变输出尺寸要用“调整图像尺寸”，真正翻转像素要用“选择 > 变换”里的翻转——两者都在后面的章节。' },
          { kind: 'h3', id: 'canvas-grid', text: '网格、对齐与相对明暗' },
          { kind: 'ul', items: ['“窗口 > 显示”分别控制像素网格、自定义网格、切片边框与相对明暗', '像素网格用于辨认单个像素；自定义网格用于格子、图块与版面，参数在“窗口 > 网格设置”', '“窗口 > 对齐”可开启网格对齐、智能对齐与辅助线；网格对齐需要自定义网格可见，智能对齐会参考画布与其他可见图层的边缘与中心', '隐藏辅助线不会关闭吸附，吸附距离在首选项设置', '“查看相对明暗”以灰度检查明暗关系，找出颜色不同但亮度过近的区域，不会把工程转成灰度模式'] },
          { kind: 'h3', id: 'canvas-tile-preview', text: '无缝平铺预览' },
          { kind: 'p', text: '打开“窗口 > 平铺预览”，选择包围平铺、X 轴或 Y 轴，软件分别显示 3×3、3×1 或 1×3 份副本。可以直接在相邻副本上绘画，结果会映射回原画布，用来跨接缝检查线条与图案是否连续。副本只是编辑视图，导出仍按原画布或所选区域进行。' },
        ],
      },
      {
        id: 'tools',
        title: '绘画工具与笔刷',
        blocks: [
          { kind: 'p', text: '工具覆盖从草图到精修的完整路径，参数都在顶部工具属性栏里调整。多子工具的按钮可以展开选择具体工具。' },
          { kind: 'h3', id: 'tools-list', text: '工具与用途' },
          { kind: 'ul', items: ['铅笔 B：按住拖动绘制；Shift 连接上一次落笔点。可调尺寸、形状、不透明度、间距、完美像素与笔刷动态', '橡皮擦 E：左键擦成透明；默认右键把与前景色完全相同的像素替换为背景色', '喷枪 J：按住持续喷涂，静止也继续；粒子半径、散布半径、密度与频率分别控制颗粒大小、范围、数量与间隔', '直线与曲线：直线拖出起终点，Shift 约束方向；曲线拖出起终点后依次固定 1–8 个控制锚点', '形状 U：矩形与椭圆的描边/填充、自由形状、多边形形状；矩形可设固定比例与圆角', '油漆桶 G：容差、连续区域、智能闭合、纹理与对称', '渐变 Shift+G：线性或径向，平滑或抖动，配合连续区域与容差；Ctrl 从落点向四周扩展，Shift 锁为圆形', '吸管 I：左键取前景色、右键取背景色，Alt 临时吸色', '平滑笔刷：沿区域涂抹整理已有像素边缘，建议局部使用并对比结果', '液化 Y：推动、膨胀、收缩与扭转，可先框选限制作用区域', '文本 T：单击创建随文字变化的文本，或拖出固定区域创建自动换行文本框'] },
          { kind: 'h3', id: 'tools-brush', text: '基础笔刷与完美像素' },
          { kind: 'p', text: '基础笔刷提供圆形、方形与线形，可在属性栏选择形状与尺寸，也可用 [ 与 ] 调整尺寸。“完美像素”清理路径折角处的多余像素，适合单像素线稿；直线阶梯设置用于均衡斜线的像素段。' },
          { kind: 'h3', id: 'tools-pattern', text: '抖动模板与图片笔刷' },
          { kind: 'ul', items: ['抖动模板：基础笔刷旁的抖动按钮可选棋盘、方向或 Bayer 模板，再用步进按钮切换密度阶段，再次点击当前模板关闭', '图片笔刷：打开“窗口 > 栏目 > 图案笔刷”，通过添加按钮或拖入图片导入；再次点击唯一选中的笔刷或切回基础形状即可退出', '从选区创建：框选后按 Ctrl+B，新笔刷保留原图颜色与透明度，成功后自动切到铅笔', '导入与创建的笔刷宽高均不能超过 256 px，按原始尺寸使用', '图片笔刷有三种对齐方式：油漆笔刷沿路径盖章、图案与来源对齐固定纹理、图案与目标对齐由首次落点决定平铺起点'] },
          { kind: 'h3', id: 'tools-dynamics', text: '压感与速度动态' },
          { kind: 'p', text: '在铅笔或橡皮擦的笔刷动态里，分别给大小、强度等效果选择压力或速度输入，并设置输入范围、输出范围、曲线与正反方向。铅笔还可以设置从背景色到前景色的动态渐变；颜色渐变 0% 对应背景色，100% 对应前景色。使用笔压时请同时确认“首选项 > 平板与触控”里的笔压开关——鼠标没有真实笔压，不能用默认压力值代替压感笔。' },
          { kind: 'h3', id: 'tools-symmetry', text: '对称绘制' },
          { kind: 'p', text: '在属性栏启用水平、垂直、斜向或旋转对称，可组合使用；拖动轴线或中心改变对称位置，在更多设置里调整轴线外观、锁定或复位中心。对称绘画会把操作映射到对应位置，选区工具也支持其可用的对称轴。矩形画布中超出有效范围的镜像部分不会写入。' },
          { kind: 'h3', id: 'tools-fill', text: '填充、智能闭合与纹理' },
          { kind: 'ul', items: ['连续模式只填相连区域，关闭后可处理相同条件的分散区域；容差越高允许的颜色差异越大', '先框选再填充，只影响真实选区内的像素', '线稿有小缺口时启用智能闭合并调整阈值，软件按虚拟边界限制填充；魔棒同样可用', '裂纹、木纹、颗粒与程序纹理从油漆桶属性中选择，并按纹理类型调整尺寸、抖动与抗锯齿'] },
        ],
      },
      {
        id: 'colors',
        title: '颜色与调色板',
        blocks: [
          { kind: 'p', text: '像素创作离不开受控的颜色，MoonSprite 把颜色组织为一等公民。' },
          { kind: 'h3', id: 'colors-picker', text: '选色与前背景色' },
          { kind: 'ul', items: ['颜色栏目可切换月环、饱和度/明度方形、色相/饱和度方形、色轮与法线调色盘', '色相吸附限制可选色相段，颜色级数限制饱和度、明度或透明度层级，适合有限色创作', '点击前景色或背景色色块打开数值编辑器，输入 HEX 或使用颜色通道', '常用模式有 HSV、RGB、LAB、GRAY，以及按项目调色板取色的 PLT；其他模式可在首选项启用', '按 X 交换前背景色；两种颜色在工程之间共享', 'HEX 支持六位颜色或带透明度的八位值，右键 HEX 输入框可读取合法剪贴板颜色'] },
          { kind: 'h3', id: 'colors-palette', text: '整理调色板' },
          { kind: 'ul', items: ['左键色格设前景色，右键设背景色；解锁编辑后可编辑、添加与删除颜色', '用 Ctrl、Shift 多选后可排序、反向排列或生成渐变；自定义布局可按二维位置摆放色格并保留空槽', '“排序与渐变”提供色调、饱和度、明度、亮度、RGB 通道与 Alpha 排序，以及升序/降序、普通渐变与色调渐变', 'Ctrl+滚轮改变色格大小', '“提取颜色”从当前画面生成色板，可新建临时色板、替换当前色板或追加颜色，并限制数量', '新色板要通过“保存色板”写入本地才能长期复用，也可保存为 PNG 色卡；内置色板不可删除'] },
          { kind: 'h3', id: 'colors-sync', text: '同步颜色与替换颜色' },
          { kind: 'ul', items: ['开启“调色板操作 > 同步颜色”后，修改色板颜色会替换全部未锁定图层与帧中 RGBA 完全相同的像素；索引模式通过色板直接改变所引用的颜色', '“编辑 > 替换颜色”（Ctrl+Shift+K）可选择来源色与替换色，并指定文档、选区、所选图层、帧、cel、调色板或循环节等范围', '吸管属性栏的“当前图层替换”与“全局替换”直接按前景色 → 背景色替换', '按住 Alt+S 后左键取色，可以把最终前景色加入调色板'] },
          { kind: 'h3', id: 'colors-modes', text: '三种颜色模式' },
          { kind: 'ul', items: ['RGBA：直接编辑颜色与透明度，适合自由绘画和半透明效果', '索引：像素引用调色板颜色，适合色板管理与有限色作品', '灰度：以灰阶绘画，适合明暗稿和灰度素材'] },
          { kind: 'p', text: '通过“图像 > 颜色模式”转换。转换会改变工程的颜色数据；只是想检查明暗时，应使用相对明暗视图。' },
        ],
      },
      {
        id: 'selection',
        title: '选区、移动与变换',
        blocks: [
          { kind: 'p', text: '选区把修改范围限定在画面的一部分，配合移动与变换完成精确的局部编辑。' },
          { kind: 'h3', id: 'selection-tools', text: '建立与组合选区' },
          { kind: 'ul', items: ['矩形 M、椭圆 Shift+M：拖动创建；Ctrl 以中心创建，Shift 在创建过程中约束比例；矩形可设圆角', '套索 Q：拖动勾勒任意轮廓，松开闭合；多边形套索 Shift+Q 逐点点击，双击、点起点或回车完成，Esc 取消', '魔棒 W：按颜色取样，可设容差、连续性与智能闭合', '固定中心绘制：矩形、椭圆选区和对应形状的属性栏提供该开关与绘制锚点。默认锚点在画布中心，可用九宫格预设、X/Y 坐标或画布上的锚点调整，并可单独控制锚点显示；启用后以该固定点向两侧创建，关闭时仍回到默认按下位置或 Ctrl 临时中心。锚点与开关属于工具偏好，不进入作品撤销', '属性栏可选新建、加选、减选与交集；开始前按住 Shift 临时加选，右键默认减选', 'Ctrl+A 全选、Ctrl+D 取消选择、Ctrl+Shift+I 反选；Ctrl+H 只隐藏蚂蚁线，选区仍然限制绘画', '矩形框选双击可选择当前网格单元，第二次按下后继续拖动可选连续单元', '时间轴中 Alt+左键点击 cel 可选中其非透明内容，Shift+Alt 加入现有选区'] },
          { kind: 'h3', id: 'selection-move', text: '移动与复制' },
          { kind: 'ul', items: ['拖选区内部移动像素，拖选框边缘只移动选框', 'Ctrl+拖动复制选区内容，Shift 约束水平或垂直方向', '确认前内容保持浮动，可继续调整；回车确认，Esc 取消当前变换', '移动工具 V 移动图层当前帧内容，开启“自动选择图层”后可按画布内容命中图层', 'Shift 点击追加 cel 选择，拖动已选内容可统一移动，Alt 拖动可复制图层', 'Ctrl+C/X/V 复制、剪切、粘贴；Ctrl+Shift+V 粘贴为新图层；“编辑 > 选择性粘贴 > 粘贴为新项目”创建独立工程', '删除选区内容会清除像素，不只是去掉选框'] },
          { kind: 'h3', id: 'selection-transform', text: '缩放、旋转与翻转' },
          { kind: 'ul', items: ['Ctrl+T 进入内容变换：拖控制点缩放，在旋转区域拖动旋转，按交互位置也可倾斜', '缩放时 Shift 保持比例、Ctrl 按整数倍、Alt 围绕轴心两侧变换；旋转时 Shift 按 45° 吸附', '“选择 > 自由变换”是另一种四角自由比例缩放入口，只保留四个角点，不用于旋转或倾斜', '“选择 > 变换”的水平/垂直翻转会实际翻转内容，默认 Shift+H / Shift+V', '多选普通图层或图层组，再选择多个帧，可统一变换这些图层与帧的组合，并保留各自图层结构', '混入文本、瓦片、自由瓦片、隐藏或锁定目标时，组合变换会被阻止'] },
          { kind: 'h3', id: 'selection-effects', text: '描边、抗锯齿与转换' },
          { kind: 'ul', items: ['“选择 > 描边”（Shift+O）为选区内内容描边；没有选区时处理当前图层的非透明内容，可设颜色、宽度、内外位置、形状与像素方向，智能色相按来源颜色生成描边', '描边与图层样式的区别：这里产生的是实际像素编辑', '“编辑 > 自动抗锯齿”根据轮廓生成过渡像素，可用自动选色、设置不透明度、选择画布或调色板颜色，并决定是否处理内部颜色边界', '自动抗锯齿可先预览，取消或关闭会恢复原像素，确认后一次撤销/重做', '“选择 > 转换为”可创建图案笔刷或背景预设；背景预设保存当前可见合成内容，非矩形选区外保持透明'] },
        ],
      },
      {
        id: 'layers',
        title: '图层、组与蒙版',
        blocks: [
          { kind: 'p', text: '图层是动画的横向轨道，帧是纵向的一列；cel 是某个图层在某一帧里的内容。' },
          { kind: 'h3', id: 'layers-manage', text: '图层管理' },
          { kind: 'ul', items: ['“图层 > 新建图层”或 Shift+N 创建；点击选择，Ctrl 增减选择，Shift 按显示顺序连续选择', '拖动排序，拖到组中归组，按 Alt 拖动复制', '眼睛控制显示，锁控制编辑；按住眼睛或锁拖过多行可连续设置，Alt 点击批量作用于同级对象', '双击或右键打开属性，修改名称、不透明度、混合模式、显示颜色与描述', '显示颜色只用于图层列表标识，不改变作品像素'] },
          { kind: 'h3', id: 'layers-mask', text: '逐帧蒙版' },
          { kind: 'p', text: '右键 cel 创建图层蒙版；右键图层或图层组，可为该对象已有内容的帧批量创建蒙版。蒙版用黑色隐藏、白色显示、灰色部分显示，彩色笔触会转为灰度；用橡皮擦恢复“未绘制”状态，未绘制区域等效于白色，原图仍然显示。' },
          { kind: 'ul', items: ['创建后在对象上方找到蒙版轨道，点击对应帧的蒙版格开始绘制', 'Alt+左键点击蒙版格，进入白底灰度的独立蒙版视图便于检查；点击普通图层、cel 或切换帧退出', '蒙版按帧独立保存，可多选、复制、粘贴、拖动、链接与断开链接', '图层组蒙版作用于整个组的合成结果', '蒙版不是画在普通图层上的黑白画稿，不会直接改掉原图像素'] },
          { kind: 'h3', id: 'layers-styles', text: '图层样式' },
          { kind: 'p', text: '右键图层或图层组 > 图层样式，选择并启用效果、调节参数、观察实时预览，应用后生效，取消则恢复原画面。' },
          { kind: 'ul', items: ['描边：宽度、内部/外部/两侧、轮廓形状与八方向开关；“智能色相”根据邻近原图颜色生成描边', '阴影：“智能阴影”根据阴影下方的背景生成深色，背景改变时随之更新', '内发光、颜色叠加、渐变叠加（平滑、Bayer 或方向抖动）', '智能描边或阴影的深色系数 0% 保持来源颜色，100% 变黑', '样式配置跨帧共享，但每帧按自己的内容计算效果；停用样式会保留参数', '要把效果变成可继续绘制的普通像素，使用“转换为 > 普通图层”'] },
          { kind: 'h3', id: 'layers-group', text: '图层组与剪贴蒙版' },
          { kind: 'ul', items: ['Ctrl+G 建组，“图层 > 新建图层组”同样可用；图层组可嵌套，能统一移动、复制、显隐、锁定或添加样式', 'Ctrl+Shift+G 解组并保留成员', '右键图层或组启用剪贴蒙版，把显示范围限制在同级紧邻下方对象的可见形状内，适合在底色轮廓里画阴影与高光', '普通且完全不透明的组按穿透方式合成；修改组不透明度或混合模式可让组整体参与合成', '“累积混合”提供另一种组与底部内容的混合计算方式'] },
          { kind: 'h3', id: 'layers-merge', text: '复制、删除与合并' },
          { kind: 'ul', items: ['复制图层包含全部帧内容；删除图层移除整条轨道；跨工程复制图层或组会保留可支持的层级、位置与属性', 'Delete 的对象取决于最后操作区域，删除前确认焦点在图层栏', '向下合并：当前图层与同级下方图层合成，逐帧保留合并结果', '合并所选图层：合并同级、连续、可见且未锁定的所选图层，逐帧生成结果', '合并图层组与合并可见图层：当前实现仍按活动帧生成结果，多帧动画请使用前两项'] },
          { kind: 'h3', id: 'layers-linked', text: '关联图层' },
          { kind: 'p', text: '右键普通像素图层 > 创建关联图层。创建后两个图层在各自对应帧共享像素：修改任一成员的绘制、擦除或颜色，其他成员同步更新；位置、名称、层级、显隐、锁定、不透明度、混合模式与样式仍可分别设置。' },
          { kind: 'ul', items: ['适合重复装饰、对照摆放，或为同一内容设置不同显示效果', '同一工程内复制关联成员仍加入原关联组；跨工程粘贴会创建独立的新关联组', '背景、文本、Tilemap 与自由瓦片不能直接创建这种关联图层', '“关联图层”连接不同图层，“关联 cel”连接同一图层的不同帧，使用时注意区分'] },
          { kind: 'h3', id: 'layers-background', text: '背景图层与类型转换' },
          { kind: 'ul', items: ['右键“新建 > 新建背景图层”，可选纯色、网格、条纹、菱形、圆形或已有预设', '背景位于根层底部、名称带下划线，仍可绘画、显隐与导出', '普通像素图层可转换为背景；扩大画布时背景按既有重复单位延展', '吸管从普通图层取色时忽略背景，以保留前景透明度；选中背景图层后可正常取背景合成色', '“转换为”可选择普通图层、背景图层或瓦片图层；转换为普通图层会把文本、瓦片或已启用样式的结果烘焙成像素', '转换可撤销，但保存为普通图片后无法恢复这些工程结构'] },
        ],
      },
      {
        id: 'animation',
        title: '动画、cel 与循环节',
        blocks: [
          { kind: 'p', text: '动画控制与帧格集成在图层栏目中。看不到帧列时，在图层设置里关闭“隐藏时间轴”——隐藏期间播放与相关操作停用，但帧数据仍然保留。' },
          { kind: 'h3', id: 'animation-frames', text: '创建、排序与播放' },
          { kind: 'ul', items: ['新增帧按钮默认复制当前帧，产生可独立编辑的副本；右键帧头可新建空白帧、复制或删除，空白帧默认 100 ms', '双击帧头修改时长；多选帧后可批量处理或拖动排序，至少保留一帧', '回车播放/暂停；逗号与句号切换前后帧，没有选区时左右方向键也可切帧', '播放中第一次按切帧键先停止，再按才切换', '右键播放按钮可选播放一次、循环全部帧、播放标签并重复，设置 0.25x–3x 倍率与停止时返回开始帧', '倍率只影响预览速度，不改写每帧保存的时长', '禁用帧保留内容供编辑，正常播放会跳过它'] },
          { kind: 'h3', id: 'animation-cel', text: 'cel 的选择、复制与关联' },
          { kind: 'ul', items: ['在帧格选择一个或多个 cel，右键复制、粘贴、删除，或拖到目标格', '复制帧包含该帧所有图层，复制 cel 只处理选中的格子', '双击有内容的 cel 可设置单元格不透明度；文本 cel 双击进入文本编辑', '关联 cel 共享内容，修改任一成员会同步同组帧；普通复制产生独立副本，适合逐帧改画', '图层与蒙版轨道的链接图标可开启自动链接，在创建流程中沿用关联内容；关闭开关不会自动断开已有链接'] },
          { kind: 'h3', id: 'animation-tween', text: '自动补间' },
          { kind: 'p', text: '右键时间轴帧头或 cel > 生成补间帧：选择含可见内容、未锁定的普通像素图层与起始帧，再选择“单帧”或“循环节”作为补间来源，设置新增帧数、每帧时长、终点水平/垂直位移、旋转角度、缩放比例与相对不透明度，选择匀速、加速、减速或先加速后减速，最后点击生成。' },
          { kind: 'ul', items: ['单帧来源中其他图层沿用起点内容；循环节来源会循环取用来源帧并叠加变化，跳过禁用帧，新帧放在来源循环节之后', '“新增帧数”包含终点帧；旋转与缩放围绕内容中心进行', '预览不包含蒙版、图层样式与混合效果', '文本、瓦片与背景图层需先转换为普通图层', '它生成的是几何与透明度过渡，不会自动重画角色的中间姿态'] },
          { kind: 'h3', id: 'animation-onion', text: '洋葱皮与循环节' },
          { kind: 'ul', items: ['在图层栏开启洋葱皮，用淡色叠加相邻帧作为动作参考；可设置显示范围、透明度、前后帧颜色，以及播放期间是否显示', '洋葱皮仅供观察，不会进入最终图片', '选择连续帧后右键帧头创建命名循环节，例如“待机”“行走”；双击括号可编辑名称、起止帧、正反方向与重复次数，拖动边界调整范围', '右键循环节“播放循环节”按其次数播放；播放按钮的“播放标签并重复”会持续循环当前帧所在的最内层循环节', '在循环节内新增帧会延续其范围；循环节也可作为 GIF、精灵表或补间的来源范围'] },
        ],
      },
      {
        id: 'tiles',
        title: 'Tilemap 瓦片地图',
        blocks: [
          { kind: 'p', text: '瓦片图层用可共享的瓦片集组织画面：同一瓦片可以在多处引用，改一次源图，所有引用同步更新。' },
          { kind: 'h3', id: 'tiles-create', text: '创建图层与复用瓦片集' },
          { kind: 'ul', items: ['右键图层 > 新建 > 新建瓦片图层，输入名称与瓦片宽高', '选择已有瓦片集时沿用其尺寸，并让多个图层共享；选择新增则建立新的瓦片集', '普通或背景图层可通过“转换为 > 瓦片图层”按网格切分全部帧，相同图块复用，边缘不足一块时补透明，画布外内容不保留', '文本或启用了样式的内容应先转为普通图层'] },
          { kind: 'h3', id: 'tiles-modes', text: '四种编辑模式' },
          { kind: 'ul', items: ['原位编辑：用像素工具直接修改已有瓦片，所有引用该瓦片的位置同步更新', '变体创建：修改当前格子的图案，创建或复用变体，只替换当前格引用', '混合编辑：在已有格中改图、在空格中绘制新内容；已有瓦片改动同步引用，空格结果创建或复用瓦片', '绘制瓦片：从瓦片集选图块按整格铺设，只修改格子引用，不修改瓦片原图'] },
          { kind: 'p', text: '新建后默认混合编辑。点击瓦片集中的实际瓦片会进入绘制瓦片；左键选择前景瓦片，右键选择背景瓦片，画布上左右键分别铺设；该模式的吸管拾取瓦片引用，橡皮擦清除格子。' },
          { kind: 'h3', id: 'tiles-manage', text: '整理与移动' },
          { kind: 'ul', items: ['瓦片集支持多选、删除、拖动调整槽位与 Ctrl+滚轮改显示大小；拖动排序不会改变画布引用', '按住 Ctrl 可查看瓦片编号；瓦片集至少保留一个瓦片', '绘制瓦片模式下，选区会扩展为整格，移动按瓦片宽高吸附', '移动整个图层只改变当前 cel 的位置，不改变瓦片原图与格子规格', '同一瓦片集可被多个图层引用，编辑前先确认模式，避免本想制作单个变体却改动了所有引用'] },
          { kind: 'h3', id: 'tiles-free', text: '自由瓦片：画一次，多处复用' },
          { kind: 'p', text: '自由瓦片适合树木、石块、角色零件与装饰物这类重复元素：实例不需要对齐固定网格，可以重叠摆放，修改源瓦片会同步更新全部引用。' },
          { kind: 'ul', items: ['右键图层 > 新建 > 新建自由瓦片图层，填写名称并选择新增集合或已有集合', '新集合会准备透明源瓦片并进入“编辑源瓦片”，直接在画布绘制，首次绘制会自动产生实例', '在瓦片集栏目选择源后切到“放置实例”，在画布单击或拖动摆放', '在实例列表选择实例，用移动工具改变位置；实例属性可设 X、Y、旋转、镜像、不透明度与混合模式', '需要改所有同类物体时，选择一个实例进入源编辑，用铅笔、橡皮擦或选区工具修改共享源', '需要另一种图案时点击“新增源瓦片”，绘制新源后再放置'] },
          { kind: 'p', text: '源保存图案，实例保存这个图案放在哪里、以什么朝向显示。实例列表支持 Ctrl 增减选择、Shift 连续选择、拖动排序、显隐、锁定、批量属性与批量删除，可独立显示或放回图层栏目。放置模式中橡皮擦删除指针下最上面的实例，吸管选择该实例的源；这一模式不使用框选与 Ctrl+T。' },
        ],
      },
      {
        id: 'image',
        title: '图像调整、滤镜与尺寸',
        blocks: [
          { kind: 'p', text: '调整类命令都会先给你实时预览，确认后才形成一次可撤销的修改。有选区时限制在当前图层的选区内；没有选区时处理所选图层。' },
          { kind: 'h3', id: 'image-adjust', text: '色彩调整' },
          { kind: 'ul', items: ['色彩平衡：分别调整阴影、中间调与高光中的青/红、洋红/绿、黄/蓝，可保持明度', '亮度/对比度：改变整体亮暗及明暗差距', '色相/饱和度 Ctrl+U：调色相、颜色浓度与 HSL 明度，明度两端趋向黑与白', '曲线 Ctrl+M：点击添加控制点、拖动改变色调关系，可分通道调整并删除中间点'] },
          { kind: 'h3', id: 'image-filters', text: '显示器风格滤镜' },
          { kind: 'ul', items: ['“编辑 > 滤镜”提供 CRT 经典隔行、RGB 荧光栅、交错栅格、VHS 色彩偏移、暗角与荧光绿辉光', '预设会生成效果图层，可通过图层显隐、不透明度或混合模式调整强度', 'LCD 屏幕滤镜需先选一个图层，再设置通道偏移宽高与扫描线类型', '滤镜用于视觉风格处理，不是画布显示缩放选项，可从历史记录撤销'] },
          { kind: 'h3', id: 'image-size', text: '画布大小、图像大小、裁切与修剪' },
          { kind: 'ul', items: ['调整画布尺寸 C：改工作边界，设置尺寸、锚点或四边边距，“同步四边”统一边距；开启“裁掉画布外的内容”才明确丢弃越界内容', '调整图像尺寸 Ctrl+Alt+I：缩放整幅内容，保留像素边缘时选最近邻，需要平滑时选双线性', '检测像素倍率：在图像尺寸窗口识别已被整数放大的像素图，识别后按倍率缩回并采用最近邻', '图像 > 裁切：以当前选区确定画布范围', '图像 > 修剪：以当前帧或所有帧的有效内容去掉透明边缘；动画需要统一边界时使用“按所有帧修剪”'] },
        ],
      },
      {
        id: 'slices',
        title: '切片与素材拆分',
        blocks: [
          { kind: 'p', text: '切片用来给画布区域命名，并按区域导出图标、按钮或其他素材。它不会自动切开原图层。' },
          { kind: 'h3', id: 'slices-create', text: '创建与管理切片' },
          { kind: 'ul', items: ['选择切片工具 Shift+C，在画布拖出区域', '选中切片后，在属性栏或切片属性中修改名称、位置与尺寸', '拖动切片或其边界调整范围；用全选与删除等命令管理切片', '规则素材表可打开“自动切片”，设置切片尺寸、起始位置与水平/垂直间距，检查预计数量后创建', '“窗口 > 显示 > 显示切片边框”控制辅助边框'] },
          { kind: 'h3', id: 'slices-export', text: '按切片导出' },
          { kind: 'p', text: '导出时把区域设为切片，再选择全部切片或指定切片；精灵表也可以使用具名切片作为区域。切片批量输出目前不使用 GIF 动画格式。' },
        ],
      },
      {
        id: 'files',
        title: '保存与导出',
        blocks: [
          { kind: 'p', text: '工程保存与成品导出是两条不同的路径：前者保留可编辑结构，后者交付给引擎、后期或客户。' },
          { kind: 'h3', id: 'files-formats', text: '选择合适的格式' },
          { kind: 'ul', items: ['.moonsprite：保存完整可编辑工程；跨电脑携带本地缩时录像时需另存并勾选携带录像', '.ase / .aseprite：与 Aseprite 交换支持的图层与动画内容，不建议作为 MoonSprite 专有数据的唯一原稿', 'PNG（自动索引或 RGBA）、JPEG、WebP、BMP：静态图片输出，不保留工程编辑结构', 'ICO：Windows 图标输出；SVG：输出矢量容器形式的像素画，不是自动识别成可编辑曲线路径', 'GIF：帧动画，受 GIF 的颜色与透明度表达限制', 'PSD：与 Photoshop 交换当前帧的图层结构；当前仅支持导出、不支持打开，不输出动画时间轴，文本与瓦片写为栅格'] },
          { kind: 'p', text: '直接打开图片后，默认开启的“沿用原格式保存”允许普通保存覆盖来源图片。新增图层、帧、文本、蒙版等原格式无法保留的结构时，软件会先列出兼容性损失，由你选择另存为 .moonsprite 或明确继续；取消不会写入文件。' },
          { kind: 'h3', id: 'files-export', text: '导出单图、区域与 GIF' },
          { kind: 'ul', items: ['“文件 > 导出 > 导出为”（Ctrl+E），选择格式、文件名与目录', '选择导出区域与可用的图层范围：画布、帧或切片', '设置倍率与修剪方式；像素展示图通常使用整数倍率', 'GIF 可选择所有帧、指定帧范围或循环节，并设置正向、反向或往复方向', '“单独修剪”分别去掉各目标的透明边缘，“统一修剪”让本批目标使用同一边界，适合保持动画对齐', '导出预设可保存常用格式、倍率、范围与路径；工程会记住成功导出的设置', 'PSD 只支持当前帧完整画布，可保留支持的组层级、显隐、透明度、混合模式、剪贴关系、蒙版与可映射样式'] },
          { kind: 'h3', id: 'files-frames', text: '导出所有帧与精灵表' },
          { kind: 'ul', items: ['“导出所有帧”按时间轴顺序生成连续编号的独立图片，复用格式、倍率与目录设置，适合引擎导入或逐帧交付', '“导出精灵表”可设置布局（水平条幅、垂直条幅、按行或按列，并可限制列数、行数、总宽或总高）', '精灵图来源可选择画布或切片、图层范围，以及全部/已选/循环节帧，并按需拆分图层或循环节', '“合并重复项”让相同帧共用一个矩形；“忽略空缺项”不为透明空帧分配区域', '拆分目标会排列并追加在同一张精灵表中，不是自动输出多个文件', '开启预览可查看排版；关闭“输出文件”会生成一个未保存的单图层工程，开启则写出 PNG'] },
        ],
      },
      {
        id: 'workspace',
        title: '工作区、分屏与预览',
        blocks: [
          { kind: 'p', text: '界面为创作服务：栏目可以按你的习惯停靠，布局本身也是工作区的一部分。' },
          { kind: 'h3', id: 'workspace-panels', text: '栏目与工作区' },
          { kind: 'ul', items: ['“窗口 > 栏目”可显示颜色、调色板、图层、历史记录、图案笔刷、瓦片集与预览', '右键栏目标题可隐藏、停靠到左右或底部，或设为悬浮；拖动标题与分隔线也可调整', '工作区管理入口可保存当前布局、切换或复位；布局修改自动保存，默认工作区不能删除', '默认数字键 1–6 分别临时呼出颜色、调色板、图层、预览、瓦片集与图案笔刷，点击外部、按 Esc 或再次触发即可收回', '工具栏可从“窗口 > 工具栏位置”放在四边；快捷指令栏在首选项中配置显示、按钮与排列'] },
          { kind: 'h3', id: 'workspace-split', text: '工程分屏与浮窗' },
          { kind: 'p', text: '把项目标签拖到画布的上、下、左、右区域，出现停靠提示后松开即可建立分屏，拖分隔线改变比例；点击某个画布后，其工程成为当前编辑目标。右键项目标签可把视图浮出为项目窗口，也能从浮窗返回标签栏。分屏、浮出与返回只改变显示位置，不会复制磁盘文件。' },
          { kind: 'h3', id: 'workspace-preview', text: '预览与历史记录' },
          { kind: 'ul', items: ['预览栏目可独立缩放，适合一边放大修像素、一边观察整体；开启“跟随画布视窗”会跟随主画布位置，手动拖动预览即退出跟随', '预览的播放按钮可独立检查动画', '历史记录栏目显示可到达的编辑步骤，点击某项即可顺序撤销或重做到该状态；退回较早状态后继续绘画会清除后续分支', '缩放、平移、镜像与布局不属于作品修改，不会出现在历史记录中'] },
        ],
      },
      {
        id: 'prefs',
        title: '首选项、主题与快捷键',
        blocks: [
          { kind: 'p', text: '从“编辑 > 首选项”打开。点击“应用”保存并保持打开，“确定”保存并关闭，“取消”放弃尚未应用的改动。分类名称可能随版本整理。' },
          { kind: 'h3', id: 'prefs-groups', text: '常用设置' },
          { kind: 'ul', items: ['常规、外观：语言、界面缩放与常用显示偏好；界面缩放与画布缩放彼此独立', '输入、编辑：滚轮与缩放方式、视图拖拽灵敏度、直线预览与阶梯算法、套索闭合预览、取色后是否返回铅笔', '平板与触控：平板接口、笔压、笔尾橡皮擦、笔侧键与画布右键动作，以及单指与双指手势', '工具、对齐：网格吸附、智能对齐、辅助线与阈值等工具行为', '光标与预览：系统或 MoonSprite 指针、指针大小、笔刷边缘与完整预览、绘制期间边缘、框选十字与尺寸提示', '背景及辅助颜色：透明棋盘格、网格、洋葱皮、对称轴与文本框颜色', '颜色与图层：选色模式、图层显示颜色预设与相关编辑偏好', '文件与恢复：默认保存格式与目录、默认导出目录、恢复间隔与保留天数、缩时默认行为', '撤销、预设、快捷指令栏、扩展与脚本，以及恢复初始设置的重置入口'] },
          { kind: 'p', text: '数值框支持简单算式，例如 32*3 或 128/2，回车或失焦后计算；Tab 与 Shift+Tab 在同一表单中切换输入框。重置会清除本地配置，但不会删除用户工程与资源文件。' },
          { kind: 'h3', id: 'prefs-theme', text: '主题' },
          { kind: 'p', text: '在主题设置中选择现有主题，或新建、复制自定义主题。编辑基础色即可调整界面配色；网格、棋盘格、洋葱皮等辅助视觉可以单独覆盖，也可以恢复为“跟随主题”。内置主题是只读的，编辑时会建立自定义副本；主题配置可导入导出，方便备份与分享。本官网的昼夜切换就来自软件的 Dark 与 Light 主题色板。' },
          { kind: 'h3', id: 'prefs-keys', text: '快捷键' },
          { kind: 'ul', items: ['“编辑 > 快捷键设置”按分类查找命令，可录入一个或多个组合，包含键盘、鼠标中键/侧键与滚轮', '每种工具还有“快速选择”项：按住临时切换，松开恢复', '修改冲突时会提示占用；工具之间共享按键可用于循环切换', '点击完成才提交整个草稿，取消不保存；快捷键配置可导入导出 JSON', '画布内滚轮快捷键只在画布区域触发，输入框中的编辑键保持输入行为'] },
        ],
      },
      {
        id: 'extensions',
        title: '扩展与运行时',
        blocks: [
          { kind: 'p', text: '.msext 是 ZIP 容器形式的扩展包，包根必须有 manifest.json，全部入口与资源都在包内。扩展不能依赖安装目录的绝对路径，也不能导入 MoonSprite 的 React、Store 或 Tauri 模块。完整字段与接口以仓库的《扩展开发》与《Extension Runtime v1》文档为准。' },
          { kind: 'h3', id: 'ext-models', text: '两套执行模型' },
          { kind: 'ul', items: ['schemaVersion: 1 是声明式 Lua 扩展，适合可撤销的文档命令、菜单与命令栏目', 'schemaVersion: 2 在兼容 Lua 命令之上，可再增加 Extension Runtime v1、宿主组件设置与扩展自有窗口；声明 runtime 时必须同时写 apiVersion: "1.0.0"', 'commands[]、panels[]、menuItems[]、topMenus[] 两个版本都能用；settingsUi 与 runtime 只属于 schema 2', 'settingsEntry 是两版都可用的受限 HTML 设置页，只在宿主组件表达不了界面时才使用'] },
          { kind: 'h3', id: 'ext-runtime', text: '运行时扩展能做什么' },
          { kind: 'ul', items: ['常驻 sandbox HTML/JavaScript：runtime.entry 是自包含的 UTF-8 页面，跑在 sandbox="allow-scripts" 的 iframe 里，没有同源权限', '窗口：windows.open 可开原生无边框附属窗口、宿主弹窗或主窗口覆盖层，位置、尺寸与命中区域由扩展声明；窗口属于主窗口，不使用系统置顶', '菜单与命令：menus.setItems 可动态替换本扩展顶层菜单的选项，菜单点击还能以 command 事件回到扩展', '工程与文档：读取工程列表、摘要、图层与帧，执行撤销重做、切换工具与主副色，但不提供任意像素写入', '存储按扩展 ID 隔离：键最长 160 字节、单值 256 KiB、总量 1 MiB；网络经宿主代理，仅 HTTP(S)、15 秒超时、单个响应体 2 MiB', '事件：project、command、interaction、clock、document-saved、export-complete、settings-changed、window-message 与 editor-event；事件是提示而非可靠队列，错过不补发', '声明的权限不会自动变成能力：启动后先调 runtime.getCapabilities()，只使用返回的 methods 与 windowPresentations'] },
          { kind: 'h3', id: 'ext-install', text: '安装与管理' },
          { kind: 'ul', items: ['在“首选项 > 扩展”中安装 .msext，也可把包拖入软件；已配置文件关联时可双击安装', '安装时先校验并解压到 staging 目录，再按扩展 ID 原子替换，更新会保留原有启用状态', '只有已启用的扩展才会贡献运行时、菜单与栏目；停用后所有入口消失，也不会被路径强行执行', '需要停止时禁用，或在扩展管理中卸载、打开扩展文件夹；停用或卸载会销毁运行时并关闭它创建的窗口', '软件没有固定的顶层“扩展”菜单，入口完全由扩展贡献决定；schemaVersion 2 的运行时扩展需要支持 Runtime v1 的宿主版本'] },
          { kind: 'h3', id: 'ext-limits', text: '边界与限额' },
          { kind: 'ul', items: ['归档上限 50 MiB、不超过 256 个文件、解压总量 256 MiB；Runtime 入口 1 MiB、设置入口 512 KiB', 'Runtime 资源最多 64 项、单项 16 MiB；命令 64 个、栏目 16 个、内置菜单贡献 32 项、顶层菜单 16 个、设置控件 64 个', 'Lua 兼容层的界面是声明式的：不能注入 React、DOM、CSS、JavaScript 或原生控件，也不能用 require 加载包内或系统文件', '任意像素或文档结构写入仍应走包内 Lua 命令，以保留事务、撤销与失败回滚', 'schema 2 的运行时无法在尚未实现 Runtime v1 的旧版中运行，扩展应做能力探测而不是假定方法存在'] },
          { kind: 'h3', id: 'ext-pet', text: '内置“宠物伴侣”' },
          { kind: 'ul', items: ['它就是一个普通 .msext 扩展，可以停用、卸载，也可以作为运行时扩展的完整例子参考', '支持多宠物、自定义动画、按条件触发的动画槽，以及 .mspet 宠物包的导入导出', '导出会把透明边缘裁掉，保持整数倍像素缩放与透明区域的点击命中', '可拖动并记住位置，提供工程信息气泡，以及报时、保存与休息提醒'] },
        ],
      },
      {
        id: 'scripting',
        title: 'Lua 脚本',
        blocks: [
          { kind: 'p', text: '普通 Lua 脚本是「一个文件丢进 scripts 文件夹」的轻量做法，适合一次性的批量处理与自定义弹窗；需要常驻界面、菜单或窗口时改用 .msext 扩展，见上一章。两者共用同一套 Lua 沙箱与事务规则。' },
          { kind: 'h3', id: 'scripting-lua', text: '运行 Lua 脚本' },
          { kind: 'ul', items: ['“文件 > 脚本 > 打开脚本文件夹”，把 UTF-8 的 .lua 文件放在该目录第一层，再重新打开文件菜单从脚本列表执行', '脚本可提供批量绘制、资源处理或自定义弹窗；不带界面、不需要安装', '支持部分 Aseprite 风格接口与专用 mse.* 接口，但并非所有 Aseprite 脚本都能直接运行', '普通像素目标、图层类型与当前帧等限制会影响可执行范围，失败时查看结果提示', '脚本不直接开放任意系统文件、网络或进程能力；首次打开时内置示例 moon-phase.lua 会放进该文件夹，已有同名文件则不覆盖'] },
          { kind: 'h3', id: 'scripting-ns', text: 'app.* 与 mse.* 的分工' },
          { kind: 'ul', items: ['app.* 是 Aseprite 兼容命名空间，用于迁移已有脚本，只实现明确列出的兼容子集', 'mse.* 是 MoonSprite 专属 API，不伪装成其他软件的 API，也不暴露内部 SpriteDocument 或 Renderer 状态', '兼容子集分两层：基础像素与图层、动画读取与编辑；脚本新建、复制、删除帧，以及在空帧中创建 Cel 尚未开放，改用 mse.* 的类型化接口', '脚本应通过 mse.apiVersion、mse.status、mse.capabilities 与 mse.isSupported() 做能力探测；当前能力表中的方法都是真实实现，没有只报错的占位端点', '兼容脚本同样运行在图像、内存、指令数与执行时间预算内，逐像素邻域扫描在较大画布上可能因预算耗尽而停止'] },
          { kind: 'h3', id: 'scripting-transaction', text: '事务与撤销' },
          { kind: 'code', text: 'local layers = mse.layers.list()\n\napp.transaction("Create palette color", function()\n  mse.palette.create { color = { r = 41, g = 121, b = 255, a = 255 } }\n  mse.layers.update(layers[1].id, { name = "Lua Layer", opacity = 192 })\nend)' },
          { kind: 'p', text: '像素修改与 mse.* 写入放进同一个事务，只形成一个撤销步骤；任一操作校验失败时整批回滚。查询方法立即返回脚本启动时的结构快照，改动副本不影响工程。创建或打开其他工程、保存、导出、导入本地笔刷与工作区显隐属于文件与应用操作，不进入当前工程的撤销历史。' },
          { kind: 'h3', id: 'scripting-help', text: '帮助与诊断' },
          { kind: 'p', text: '“帮助”菜单提供组件库、更新日志、使用统计、诊断日志与关于入口。遇到问题时记录软件版本、复现步骤与相关提示，再按需打开诊断日志。使用统计不影响画稿内容。' },
        ],
      },
      {
        id: 'compatibility-api',
        title: 'Aseprite 兼容层',
        blocks: [
          { kind: 'p', text: 'app.* 只实现项目明确列出的兼容子集，目的是让已有脚本能迁移过来，不代表完整兼容 Aseprite。新写的脚本建议直接用 mse.*：类型化接口更全，也没有下面这些语义陷阱。本页按当前实现列出成员与限制，完整签名以仓库的 Lua 兼容 API 文档为准。' },
          { kind: 'h3', id: 'compat-globals', text: '全局与 app' },
          { kind: 'ul', items: ['构造器：Point、Rectangle、Color、Image、Palette、Sprite、Dialog；Sprite(width, height) 创建的是暂存文档，宿主在应用结果时才真正建立', 'app.activeSprite / sprite / activeImage / activeLayer / activeCel / activeFrame / frame / range 组成当前脚本上下文，帧序号从 1 开始', 'app.fgColor 与 app.bgColor 是颜色快照；给 app 的普通表赋值不会调用任何宿主命令', 'app.transaction([label,] fn) 把排队的编辑合成一个事务并返回回调结果，这是所有写入的入口', 'app.useTool 只接受 line 与 eraser，points 用文档坐标；其他工具名会被拒绝', 'app.alert() 只是把文字追加到脚本结果，不是阻塞式原生选项弹窗；app.refresh() 与 app.command.Undo/Redo() 是兼容占位，不做实际操作', 'app.pixelColor 提供 rgba / rgbaR·G·B·A 与 graya / grayaV·A / index，按图像颜色模式选用'] },
          { kind: 'h3', id: 'compat-members', text: '对象成员范围' },
          { kind: 'ul', items: ['点、矩形与颜色：Point 的 x/y 可写；Rectangle 全只读；Color 的 red/green/blue/alpha 与 hsv 分量可写', '图像与调色板：Image 支持 getPixel / drawPixel / putPixel / clear / drawImage / clone，使用图像局部坐标且要求颜色模式一致；Palette 的 getColorIndex 返回最近颜色的条目 ID，不是数组下标', '图层与 Cel：Layer 可写 name、opacity、isVisible、isEditable、isLocked 等字段，Layer:cel(frame) 取出 Cel；已有 Cel 可通过 image 与 position 编辑', '帧与标签：Frame 可写 duration（单位是秒，提交时换算到 1–60000 毫秒，与 mse.* 的毫秒不同），isDisabled 只读；Tag 的 repeats = 0 表示无限重复，aniDir 只把 1 当反向', '选区：Selection 只提供 isEmpty、bounds 与 contains 三个只读查询，要改选区请用 mse.selection', 'Sprite 提供 width/height/colorMode/palette/frames/layers/tags/selection 等读取入口，可写的只有 name'] },
          { kind: 'h3', id: 'compat-gaps', text: '明确的缺口' },
          { kind: 'ul', items: ['没有通用的帧新建、复制与删除；需要增删帧时用 mse.* 的接口，或围绕已有帧与 Cel 设计脚本', 'Sprite:newCel 只能作用于脚本自己创建的图层（含新建 Sprite），不能传入现有宿主图层，也不能在空帧里新建 Cel', 'Layer 的 isContinuous、stackIndex、parent 三个 setter 只改 Lua 包装对象，不会排队写入宿主；parent 是组 ID 字符串或 nil，不是 Layer 对象', '没有 require，也没有任意文件、网络或进程访问，更没有完整的 Aseprite 命令注册表', 'ColorMode 常量里有 TILEMAP，但常量存在不代表能写瓦片图像'] },
          { kind: 'h3', id: 'compat-dialog', text: 'Dialog 控件' },
          { kind: 'ul', items: ['Dialog { title?, onclose? } 支持 button、check、color、combobox、entry、label、number、radio、separator、slider', '通用选项为 id、label、text、enabled（默认 true）与 visible（默认 true）；回调是 onclick、onchange、onrelease', '取值方式：check 与 radio 用 selected，color 用 color，combobox 用 options 与 option，number 用 value（也可解析数值型 text）配合 min、max、step、decimals', 'dlg.data 按控件 ID 读取当前值，dlg:modify { id, ... } 改控件，dlg:close() 关闭', 'dlg:show { wait = false } 在首次执行结束后保留回调；省略 wait 走阻塞流程', 'newrow() 与 repaint() 是无操作，bounds 返回零矩形而不是真实几何；它与 Runtime 的宿主表单、清单设置不是同一套协议'] },
          { kind: 'h3', id: 'compat-budget', text: '沙箱额度' },
          { kind: 'p', text: '脚本 1 MiB、图像与修改像素 4,194,304、Lua 内存 64 MiB、输出 64 KiB、指令数 20,000,000、执行预算 2 秒。这些是安全额度而不是性能承诺：逐像素扫描大画布时，脚本可能因为指令数或时间耗尽而停止，那不是语法错误。' },
        ],
      },
      {
        id: 'mse-api',
        title: 'MSE API 参考',
        blocks: [
          { kind: 'p', text: 'MSE（MoonSprite Extension）是 MoonSprite 的专属 Lua API，当前版本 0.2.0，处于 experimental 阶段。新脚本应当优先使用它：类型化接口覆盖得更全，也没有兼容层的语义陷阱。要迁移已有 Aseprite 脚本时看上一章。本页列出各模块的常用端点与约定；完整类型提示见仓库中的 mse-api.lua，可加入 VS Code 的 LuaLS 工作区库路径。' },
          { kind: 'h3', id: 'mse-model', text: '执行模型' },
          { kind: 'ul', items: ['查询方法立即返回脚本启动时的结构快照，返回的表是副本，修改副本不会影响工程', '写入方法通过参数校验后返回 true，并加入当前脚本事务', '同一事务中的像素修改与 mse 写入只形成一个撤销步骤；任一操作失败时整批回滚', '创建或打开工程、保存、导出、导入本地笔刷、栏目布局属于文档外操作，不进入撤销历史', '颜色使用 { r, g, b, a }，通道范围 0-255；图层不透明度同为 0-255', 'ID 区分大小写；帧参数可用稳定帧 ID 或从 1 开始的帧序号'] },
          { kind: 'h3', id: 'mse-capabilities', text: '能力发现' },
          { kind: 'code', text: 'print(mse.apiVersion)                       -- "0.2.0"\nprint(mse.status.stage)                     -- "experimental"\nprint(mse.capabilities.layers.status)       -- "stable"\nprint(mse.isSupported("tiles.createLayer")) -- true' },
          { kind: 'p', text: 'mse.capabilities 的每个模块包含 status（当前为 stable）、readOnly 与 methods（方法名称、implemented、readOnly）。当前能力表中的所有方法均为真实实现，没有只报错的占位端点。' },
          { kind: 'h3', id: 'mse-document', text: 'mse.document' },
          { kind: 'ul', items: ['info()：返回当前文档与活动图层摘要（id、name、filePath、width、height、colorMode、frame、activeLayer）', 'activeLayer()：返回活动图层的结构信息，包括类型、位置、尺寸、不透明度、混合模式、可见性、锁定与样式', 'create(spec)：新建工程，可设置 name、width、height、colorMode（rgba / grayscale / indexed）', 'open(path?)：传入路径打开文件，省略时打开文件选择器', 'save(spec?)：保存活动工程，{ saveAs = true } 为另存为'] },
          { kind: 'code', text: 'mse.document.create {\n  name = "Sprite",\n  width = 64,\n  height = 64,\n  colorMode = "rgba"\n}' },
          { kind: 'h3', id: 'mse-layers', text: 'mse.layers' },
          { kind: 'ul', items: ['list() / get(id)：返回普通、文本、瓦片与自由瓦片图层的结构信息', 'create(spec?)：创建空白普通图层，可设置 name 与 opacity', 'duplicate(id) / remove(id)：复制或删除图层，沿用 cel、共享资源与锁定检查', 'update(id, patch)：更新 name、opacity、blendMode、visible、locked、x、y、description、displayColor'] },
          { kind: 'code', text: 'local layers = mse.layers.list()\napp.transaction("Rename layer", function()\n  mse.layers.update(layers[1].id, { name = "Outline", opacity = 192 })\nend)' },
          { kind: 'h3', id: 'mse-animation', text: 'mse.animation' },
          { kind: 'ul', items: ['frames()：返回 { id, number, duration, active }[]', 'setFrame(frame)：切换活动帧', 'loops() / createLoop(spec) / updateLoop(id, spec) / removeLoop(id)：管理动画循环节', 'play(id?)：传入循环节 ID 播放该循环节，省略时播放全部动画'] },
          { kind: 'p', text: '循环节参数：name、start（或 startFrameId）、["end"]（或 endFrameId）、direction（forward / reverse）、repeatCount（nil 表示无限重复）。' },
          { kind: 'h3', id: 'mse-palette', text: 'mse.palette' },
          { kind: 'ul', items: ['list() / get(id)：查询调色板颜色', 'create(spec)：{ color = color }，也可以直接传颜色表', 'update(id, patch)：更新 color 或 name', 'remove(id)：删除颜色', 'extract(spec?)：从当前合成图像提取颜色，limit 为 1-4096，mode 为 replace 或 append'] },
          { kind: 'h3', id: 'mse-tiles', text: 'mse.tiles 与 mse.freeTiles' },
          { kind: 'ul', items: ['tiles.listSets() / getSet(id)：返回瓦片集名称、网格尺寸、布局与稳定瓦片 ID；预算内还会返回打包 RGBA pixels', 'tiles.createSet(spec)：创建瓦片集，如 { name = "Terrain", tileWidth = 16, tileHeight = 16 }', 'tiles.createLayer(spec)：创建瓦片图层，tilesetId 可省略以新建', 'tiles.place(spec)：在 Tilemap Cel 的指定单元格放置或清除瓦片，支持 rotation 与翻转', 'tiles.edit(spec)：替换一个瓦片的 RGBA 像素', 'freeTiles.listSources() / createLayer(spec) / createSource(spec) / place(spec) / edit(spec)：管理自由瓦片图层与共享源，编辑源后全部实例同步更新'] },
          { kind: 'h3', id: 'mse-brushes', text: 'mse.brushes' },
          { kind: 'ul', items: ['list() / get(id)：返回工程笔刷与本地笔刷，预算内包含 coverage 与 colors', 'importImage()：打开图片选择器并导入本地图案笔刷', 'createFromSelection()：从当前选区创建工程笔刷', 'remove(id)：删除工程笔刷或已列出的本地笔刷', '脚本不能读取任意笔刷文件路径或原始文件字节'] },
          { kind: 'h3', id: 'mse-selection', text: 'mse.selection' },
          { kind: 'ul', items: ['info()：返回 exists、empty、hasMask、selectedPixels 与 bounds', 'set(spec)：设置矩形选区，可选 width * height 的 0-255 掩码', 'clear() / invert()：清除或反选', 'transform(spec)：支持 dx、dy、flipHorizontal、flipVertical，复用现有选区移动与翻转历史'] },
          { kind: 'code', text: 'mse.selection.set { x = 4, y = 8, width = 16, height = 12 }' },
          { kind: 'h3', id: 'mse-slices-styles', text: 'mse.slices 与 mse.styles' },
          { kind: 'ul', items: ['slices：list() / get(id) / create(spec) / update(id, patch) / remove(id)，管理命名导出切片', 'styles.get(layerId?)：查询指定或活动图层的样式', 'styles.apply(layerId, styles)：应用完整样式模型，非法尺寸、方向、颜色或枚举会被规范化或拒绝', 'styles.copy(layerId) / paste(layerId)：使用软件共享的图层样式剪贴板', 'styles.clear(layerId) / setEnabled { id, enabled }：清除或启停样式'] },
          { kind: 'h3', id: 'mse-workspace-io', text: 'mse.workspace 与 mse.io' },
          { kind: 'ul', items: ['workspace.listPanels() / getPanel(id)：查询栏目 visible 与 dock', 'workspace.showPanel(id) / hidePanel(id) / setPanel { id, visible?, dock? }：dock 为 left、right、bottom 或 floating；栏目布局不写入文档历史', 'io.open(path?) / io.save(spec?)：等同 document 的同名方法', 'io.export(spec)：使用软件现有导出模型，可设置 name、format、directory、target、缩放与 GIF 参数'] },
          { kind: 'h3', id: 'mse-ui', text: 'mse.ui' },
          { kind: 'ul', items: ['notify(text)：在软件状态区显示消息', 'alert(value)：兼容 app.alert，文本进入脚本结果提示', 'dialog(options)：创建组件化对话框，支持现有回调与持久会话'] },
          { kind: 'code', text: 'local dlg = mse.ui.dialog { title = "Options" }\ndlg:number { id = "amount", label = "Amount", value = 4 }\ndlg:button { text = "Apply", onclick = function()\n  mse.ui.notify("Applied: " .. dlg.data.amount)\nend }\ndlg:show { wait = false }' },
          { kind: 'h3', id: 'mse-safety', text: '安全边界' },
          { kind: 'ul', items: ['Lua 不能直接访问文件系统、网络、进程、调试库、DOM、React、原始 Store、历史栈或任意 Tauri 命令', '文件选择、保存、导出与资源导入只能经过本 API 明确开放的受控入口', '像素与结构写入受目标身份、revision、尺寸、内存、指令数、执行时间与撤销校验保护'] },
        ],
      },
      {
        id: 'shortcuts',
        title: '默认快捷键速查',
        blocks: [
          { kind: 'p', text: '以下是默认配置。修改过快捷键时，以软件内“编辑 > 快捷键设置”显示的为准；每种工具还支持按住即临时切换的“快速选择”键。' },
          { kind: 'h3', id: 'shortcuts-file', text: '文件与编辑' },
          { kind: 'ul', items: ['新建 Ctrl+N ／ 打开 Ctrl+O ／ 保存 Ctrl+S ／ 另存为 Ctrl+Shift+S', '导出 Ctrl+E ／ 关闭当前工程 Ctrl+W', '撤销 Ctrl+Z ／ 重做 Ctrl+Shift+Z', '复制 Ctrl+C ／ 剪切 Ctrl+X ／ 粘贴 Ctrl+V ／ 粘贴为新图层 Ctrl+Shift+V', '色相饱和度 Ctrl+U ／ 曲线 Ctrl+M ／ 替换颜色 Ctrl+Shift+K'] },
          { kind: 'h3', id: 'shortcuts-tools', text: '工具' },
          { kind: 'ul', items: ['铅笔 B ／ 橡皮擦 E ／ 喷枪 J ／ 液化 Y', '框选 M ／ 椭圆框选 Shift+M ／ 套索 Q ／ 多边形套索 Shift+Q', '魔棒 W ／ 移动 V ／ 形状 U ／ 文本 T', '填充 G ／ 渐变 Shift+G ／ 吸管 I ／ 交换颜色 X', '抓手 H ／ 缩放 Z ／ 旋转视图 R ／ 临时抓手 Space ／ 临时吸管 Alt', '切片 Shift+C ／ 画布尺寸 C ／ 图像尺寸 Ctrl+Alt+I ／ 减小与增大笔刷 [ 和 ]'] },
          { kind: 'h3', id: 'shortcuts-selection', text: '选区、图层与动画' },
          { kind: 'ul', items: ['全选 Ctrl+A ／ 取消选择 Ctrl+D ／ 反选 Ctrl+Shift+I ／ 隐藏蚂蚁线 Ctrl+H', '变换 Ctrl+T ／ 描边 Shift+O ／ 水平翻转内容 Shift+H ／ 垂直翻转内容 Shift+V', '新建图层 Shift+N ／ 新建组 Ctrl+G ／ 解组 Ctrl+Shift+G ／ 由选区创建图案笔刷 Ctrl+B', '播放或暂停 Enter ／ 前一帧 , ／ 后一帧 .', '填充前景色 F ／ 相对明暗 Ctrl+Y（注意：重做是 Ctrl+Shift+Z）', '显示或隐藏图层栏目 Tab ／ 临时栏目 1–6 ／ 全屏 F11 ／ 高级模式 Ctrl+F'] },
        ],
      },
      {
        id: 'support',
        title: '客户支持',
        blocks: [
          { kind: 'p', text: '开发阶段的问题与建议都会直接进入开发流程。' },
          { kind: 'h3', id: 'support-issues', text: '问题反馈' },
          { kind: 'p', text: '在 GitHub Issues 提交问题时，附上软件版本、复现步骤与相关提示可以显著加快定位；崩溃类问题请尽量说明操作路径。' },
          { kind: 'h3', id: 'support-community', text: '社区与讨论' },
          { kind: 'p', text: '功能讨论与使用交流在 GitHub Discussions 进行；哔哩哔哩、小红书等社区渠道随发布逐步开放。' },
          { kind: 'h3', id: 'support-release', text: '更新与发布' },
          { kind: 'p', text: '“帮助 > 更新日志”说明最近已打包版本的变更；博客会不定期发布开发进展与设计笔记。发布渠道、授权与定价信息随正式发布同步公布。' },
        ],
      },
    ],
  },
  en: {
    title: 'Documentation',
    subtitle: 'Every MoonSprite feature, in the same order as the user guide: what it is for, where it lives, and how to use it.',
    outline: [
      { kind: 'page', id: 'intro' },
      { kind: 'page', id: 'quickstart' },
      { kind: 'group', id: 'g-part1', title: 'Creation basics', children: ['start', 'canvas', 'tools', 'colors', 'selection'] },
      { kind: 'group', id: 'g-part2', title: 'Structure and animation', children: ['layers', 'animation'] },
      { kind: 'group', id: 'g-part3', title: 'Tiles and images', children: ['tiles', 'image', 'slices'] },
      { kind: 'group', id: 'g-part4', title: 'Delivery and workspace', children: ['files', 'workspace', 'prefs'] },
      { kind: 'group', id: 'g-part5', title: 'Extensions and reference', children: ['extensions', 'scripting', 'mse-api', 'compatibility-api', 'shortcuts'] },
      { kind: 'page', id: 'support' },
    ],
    sections: [
      {
        id: 'intro',
        title: 'Meet MoonSprite',
        blocks: [
          { kind: 'p', text: 'MoonSprite is an original pixel art workstation for Windows that brings drawing, frame-by-frame animation, tile workflows, extensions, and final delivery into one workspace. It is built with Tauri 2, React, TypeScript, Zustand, and Canvas, runs natively on Windows 10 / 11, and never needs a network connection.' },
          { kind: 'h3', id: 'intro-features', text: 'Signature features' },
          { kind: 'p', text: 'These are the workflows MoonSprite invests the most in, and the ones worth learning first. Frame-by-frame animation, tilemaps, onion skin, pattern brushes, split view, and scripting are just as complete — they are simply not unique to this app.' },
          { kind: 'ul', items: ['Free tiles: reusable patterns placed as instances that may overlap and sit anywhere; edit the source once and every reference updates', 'Masks: hide with black, reveal with white and grey, keep the original pixels, and give every frame its own mask', 'Layer styles: outline, drop shadow, inner glow, color and gradient overlays that follow the artwork live, with nothing redrawn by hand', 'Editable text: keep the characters, font, and layout, and return to the text layer later; fixed boxes wrap automatically', 'Automatic tweening: generate transition frames from end-point offset, rotation, scale, and opacity instead of redrawing every frame', 'Linked layers: several layers share one pixel drawing while keeping their own position, visibility, and display effects', 'Timelapse recording: keep the drawing process automatically, export it as MP4, WebM, or an image sequence, and carry it with the project', 'Liquify and ISO guides: push, expand, contract, and twist to clean up contours; ISO view constrains lines to the isometric step'] },
          { kind: 'h3', id: 'intro-compare', text: 'How it compares with Aseprite' },
          { kind: 'p', text: 'This comparison covers only the built-in features of the official stable Aseprite release, checked against its official documentation, roadmap, and release notes on 19 September 2026. Third-party scripts and extensions do not count as built-in. The point is to describe workflow differences, not to claim that the official release will never add the same abilities.' },
          { kind: 'ul', items: ['Per-frame masks and group masks: control visibility in black, white, and grey while keeping the original pixels, with a different mask per frame', 'Editable text layers: keep the characters, font, and layout, and still be able to change them later', 'Live layer styles: outline, shadow, inner glow, and color effects that follow the source artwork', 'Automatic tween frames: generate in-between frames from offset, rotation, scale, and opacity', 'Free tiles: instances that overlap and sit anywhere while sharing one source image', 'Cross-layer linked content: layers that share pixels while keeping their own position and display attributes'] },
          { kind: 'h3', id: 'intro-requirements', text: 'Requirements and licensing' },
          { kind: 'ul', items: ['Windows 10 or Windows 11 with the WebView2 Runtime (the installer handles it)', 'Source-available; official binaries distributed through Steam and authorized channels are licensed per seat for personal and commercial work', 'The whole creation workflow runs locally, with no network connection required', 'The interface ships in Simplified Chinese, English, Japanese, Korean, Spanish, French, German, Brazilian Portuguese, and Russian'] },
        ],
      },
      {
        id: 'quickstart',
        title: 'First artwork in five minutes',
        blocks: [
          { kind: 'p', text: 'New project to export is seven steps. Animation and tiles can wait until this path feels natural.' },
          { kind: 'h3', id: 'quickstart-new', text: 'Create a project' },
          { kind: 'p', text: 'Press Ctrl+N, enter a name and canvas size, and pick a color mode; RGBA is the right start for freehand work. If the clipboard holds an image, the new-project window uses its dimensions as the starting values.' },
          { kind: 'h3', id: 'quickstart-draw', text: 'Make the first marks' },
          { kind: 'ul', items: ['Pick a foreground color in the color panel, press B for the pencil, and drag with the left button', 'Press E to erase; scroll to zoom, hold Space to pan', 'Press Shift+N to add a layer and separate line art, color, and detail', 'Press M to select, drag to move, or press Ctrl+T to transform, then Enter to confirm', 'Press Ctrl+S to save a .moonsprite and keep layers and editable data', 'Press Ctrl+E to export a PNG, choosing scale and folder'] },
          { kind: 'p', text: 'Painting writes into the single cell at the current layer and current frame — it does not change every frame. When you are ready to animate, add frames in the layer panel and edit them one by one.' },
          { kind: 'h3', id: 'quickstart-workspace', text: 'Meet the workspace' },
          { kind: 'p', text: 'The canvas sits in the middle, with dockable and floating panels around it: color, palette, layers with the timeline, preview, history, tile set, and pattern brushes. Pressing 1–6 calls up a panel temporarily; clicking outside or pressing Esc puts it back without disturbing the layout.' },
          { kind: 'h3', id: 'quickstart-next', text: 'Where to go next' },
          { kind: 'ul', items: ['To see what makes MoonSprite different, read “Meet MoonSprite”', 'To learn drawing systematically, start at “Projects and files” and read down', 'To solve one specific problem, jump to the FAQ'] },
        ],
      },
      {
        id: 'start',
        title: 'Projects and files',
        blocks: [
          { kind: 'p', text: 'MoonSprite organizes work as projects: the home screen helps you find them again, and the File menu moves them in and out.' },
          { kind: 'h3', id: 'start-open', text: 'New and open' },
          { kind: 'ul', items: ['“File > New” sets the canvas, color mode, and whether to record the drawing process', '“File > Open”, Ctrl+O, or drag a file in from Explorer', 'Openable formats: .moonsprite, .ase, .aseprite, PNG, JPEG, WebP, BMP, GIF', 'Animated GIFs import as multi-frame projects, keeping the readable frame order, durations, and loop information', 'Several projects stay open as tabs; Ctrl+W closes the current one after prompting about unsaved work'] },
          { kind: 'h3', id: 'start-home', text: 'Home, gallery, and custom folders' },
          { kind: 'p', text: 'Home offers recent, gallery, and recovery panels. Click a project card to open it, right-click a normal card to reveal its folder, and use Ctrl+scroll to change the list size.' },
          { kind: 'ul', items: ['Panel management can add custom folders and browse the first level of supported projects and images in that directory; panels can be reordered by dragging', 'Removing a custom panel removes the entry only and never deletes files', '“Delete project” in the gallery deletes the file from disk after confirmation, unlike “Remove from recent”'] },
          { kind: 'h3', id: 'start-recovery', text: 'Recovery and project rollback' },
          { kind: 'ul', items: ['After an unexpected exit, open the draft from Home > Recovery, check it, then save it properly; opening or closing a draft does not delete the recovery record right away', '“Preferences > Files and recovery” sets the autosave interval and how many days to keep data — 7 by default, adjustable from 1 to 365', 'Automatic recovery data is not a substitute for saving your project', 'With project backup enabled, saved projects can be restored from “File > Project rollback”; check the backup time first, and use history to undo the result', '“File > Project info” shows size, color mode, layer/group/frame counts, strokes and edit counts, effective drawing time, and how many timelapse records exist'] },
          { kind: 'h3', id: 'start-timelapse', text: 'Timelapse recording' },
          { kind: 'p', text: 'Tick “Record drawing process” when creating a project, or set recording from “File > Timelapse video”. Full recording keeps every entry; smart sampling gradually thins older entries while keeping recent changes, and switching modes never deletes existing records.' },
          { kind: 'ul', items: ['Recording follows committed content operations — it ignores pauses, zooming, panning, and panel dragging', 'Enable “Record undo steps” when the revision process itself should be captured', 'Export as MP4, WebM, or a PNG/JPG sequence; video supports size, quality, and a target duration or speed', 'Installed builds keep recordings in the local library; when moving to another computer, save as and tick “Carry timelapse recordings”, because a normal save does not guarantee they travel', 'Clearing records does not delete the artwork, but it cannot be undone and does not free disk space already used by the local library'] },
        ],
      },
      {
        id: 'canvas',
        title: 'Canvas and guides',
        blocks: [
          { kind: 'p', text: 'View operations change how you look at the artwork. They never modify pixels and never enter undo history.' },
          { kind: 'h3', id: 'canvas-view', text: 'Zoom, pan, rotate, and mirror' },
          { kind: 'ul', items: ['Zoom: the scroll wheel or the zoom tool Z; while drag-zooming, hold Shift to snap through scale steps', 'Pan: hand tool H, Space+drag, or middle-button drag', 'Rotate view: rotate tool R, or “Window > Rotate view”, to make a stroke easier to draw', 'Mirror view: “Window > Mirror view” checks proportion and composition without flipping the pixels', 'Reset and display: “Window > Reset view”, full screen, and advanced mode (Ctrl+F)'] },
          { kind: 'p', text: 'Changing the real output size is “Resize image”, and really flipping pixels is the flip commands under “Select > Transform” — both covered later.' },
          { kind: 'h3', id: 'canvas-grid', text: 'Grid, snapping, and relative luminance' },
          { kind: 'ul', items: ['“Window > Show” controls the pixel grid, the custom grid, slice borders, and relative luminance separately', 'The pixel grid identifies single pixels; the custom grid is for cells, tiles, and layout, configured in “Window > Grid settings”', '“Window > Snap” turns on grid snapping, smart snapping, and guides; grid snapping needs the custom grid visible, and smart snapping references the canvas and the edges and centers of other visible layers', 'Hiding guides does not turn snapping off, and the snap distance lives in preferences', '“View relative luminance” reads the artwork in grey to expose areas that differ in color but sit too close in brightness; it does not convert the project to grayscale'] },
          { kind: 'h3', id: 'canvas-tile-preview', text: 'Seamless tile preview' },
          { kind: 'p', text: 'Open “Window > Tiled preview” and choose surrounding, X-axis, or Y-axis tiling to show 3×3, 3×1, or 1×3 copies. You can paint directly on a neighbouring copy and the result maps back to the original canvas, which is how you check lines and patterns across a seam. Copies are an editing view only: export still follows the original canvas or the selected region.' },
        ],
      },
      {
        id: 'tools',
        title: 'Drawing tools and brushes',
        blocks: [
          { kind: 'p', text: 'The tools cover the whole path from sketch to polish, and every parameter sits in the options bar at the top. Buttons with several sub-tools expand so you can pick a specific one.' },
          { kind: 'h3', id: 'tools-list', text: 'Tools and what they do' },
          { kind: 'ul', items: ['Pencil B: drag to draw; Shift connects from the previous point. Size, shape, opacity, spacing, pixel-perfect, and brush dynamics are adjustable', 'Eraser E: the left button erases to transparent; by default the right button replaces pixels identical to the foreground color with the background color', 'Airbrush J: sprays while held and keeps spraying when still; particle radius, spread radius, density, and frequency control grain size, range, amount, and interval', 'Line and curve: the line drags out start and end points with Shift constraining direction; the curve sets endpoints first, then fixes 1–8 control anchors in turn', 'Shape U: rectangle and ellipse with stroke or fill, free shapes, and polygon shapes; rectangles support fixed ratios and rounded corners', 'Paint bucket G: tolerance, contiguous area, smart close, texture, and symmetry', 'Gradient Shift+G: linear or radial, smooth or dithered, with contiguous area and tolerance; Ctrl expands from the drop point in all directions and Shift locks it to a circle', 'Eyedropper I: left button takes the foreground color, right button the background color, and Alt samples temporarily', 'Smoothing brush: paint along an area to tidy existing pixel edges; use it locally and compare the result', 'Liquify Y: push, expand, contract, and twist, optionally limited by a selection first', 'Text T: click for text that sizes to its content, or drag a fixed box for automatic wrapping'] },
          { kind: 'h3', id: 'tools-brush', text: 'Base brushes and pixel-perfect' },
          { kind: 'p', text: 'Base brushes come in circle, square, and line shapes, chosen with shape and size in the options bar or by pressing [ and ]. “Pixel-perfect” cleans up the redundant pixels at the corners of a path, which matters for single-pixel line art; the line step setting balances the pixel runs of a diagonal. The pencil, eraser, and line tools each keep their own parameters.' },
          { kind: 'h3', id: 'tools-pattern', text: 'Dither templates and image brushes' },
          { kind: 'ul', items: ['Dither templates: the dither button next to the base brush offers checkerboard, directional, or Bayer patterns, and the step button moves through density stages; clicking the active pattern again turns it off', 'Image brushes: open “Window > Panels > Pattern brushes” and import with the add button or by dragging images in; click the only selected brush again, or switch back to a base shape, to leave image-brush mode', 'From a selection: select an area and press Ctrl+B; the new brush keeps the original colors and transparency, and the pencil becomes active on success', 'Imported and created brushes may not exceed 256 px in width or height, and are used at their original size', 'Image brushes align in three ways: paint brush stamps along the path, pattern-aligned-to-source keeps the texture fixed in source coordinates, and pattern-aligned-to-target starts tiling from the first drop point'] },
          { kind: 'h3', id: 'tools-dynamics', text: 'Pressure and speed dynamics' },
          { kind: 'p', text: 'In the pencil or eraser brush dynamics, choose pressure or speed as the input for size, strength, and other effects, then set input range, output range, curve, and direction. The pencil can also run a dynamic gradient from the background color to the foreground color, where 0% maps to the background and 100% to the foreground. When using pen pressure, check the pressure switch in “Preferences > Tablet and touch” — a mouse has no real pressure and cannot stand in for a pressure-sensitive pen. Image brushes keep their fixed size but still respond to the strength and color dynamics that apply.' },
          { kind: 'h3', id: 'tools-symmetry', text: 'Symmetry' },
          { kind: 'p', text: 'Enable horizontal, vertical, diagonal, or rotational symmetry in the options bar and combine them freely; drag the axis or center to move the symmetry, and use the additional settings to change the axis appearance, lock it, or reset the center. Symmetric painting maps each operation to the mirrored position, and selection tools support the axes available to them. On a rectangular canvas, mirrored parts that fall outside the valid range are not written.' },
          { kind: 'h3', id: 'tools-fill', text: 'Fill, smart close, and textures' },
          { kind: 'ul', items: ['Contiguous mode fills only connected areas; turning it off reaches scattered areas that match the same conditions, and a higher tolerance accepts a wider color difference', 'Selecting an area before filling limits the result to pixels actually inside the selection', 'When line art has small gaps, enable smart close and adjust the threshold so the fill respects a virtual boundary; the magic wand can use it too', 'Cracks, wood grain, grain, and procedural textures come from the paint bucket properties, with size, jitter, and antialiasing per texture type'] },
        ],
      },
      {
        id: 'colors',
        title: 'Color and palettes',
        blocks: [
          { kind: 'p', text: 'Pixel art depends on controlled color, so MoonSprite treats color as a first-class citizen.' },
          { kind: 'h3', id: 'colors-picker', text: 'Picking colors and the foreground/background pair' },
          { kind: 'ul', items: ['The color panel switches between moon ring, saturation/value square, hue/saturation square, color wheel, and normal palette', 'Hue snapping limits the selectable hue band and color steps limit saturation, value, or alpha levels — useful for limited-palette work', 'Click the foreground or background swatch to open the numeric editor and enter HEX or use color channels', 'Common modes are HSV, RGB, LAB, GRAY, and PLT for picking from the project palette; other modes can be enabled in preferences', 'Press X to swap foreground and background; both colors are shared across projects', 'HEX accepts six digits or eight with alpha, and right-clicking the HEX field reads a valid color from the clipboard'] },
          { kind: 'h3', id: 'colors-palette', text: 'Organizing palettes' },
          { kind: 'ul', items: ['Left-click a swatch to set the foreground color and right-click for the background color; unlock editing to edit, add, and delete colors', 'Select with Ctrl and Shift, then sort, reverse, or generate a gradient; a custom layout places swatches by two-dimensional position and keeps empty slots', '“Sort and gradient” offers hue, saturation, value, brightness, RGB channel, and alpha sorting, plus ascending/descending, plain gradients, and hue gradients', 'Ctrl+scroll changes swatch size', '“Extract colors” builds a palette from the current artwork — create a temporary palette, replace the current one, or append colors, with a count limit', 'A new palette must be written locally with “Save palette” to be reusable, and it can also be saved as a PNG color card; built-in palettes cannot be deleted'] },
          { kind: 'h3', id: 'colors-sync', text: 'Sync color and replace color' },
          { kind: 'ul', items: ['With “Palette operations > Sync color” on, changing a palette color replaces pixels of exactly the same RGBA across all unlocked layers and frames; indexed mode changes the referenced color through the palette directly', '“Edit > Replace color” (Ctrl+Shift+K) chooses a source and replacement color, and a scope: document, selection, selected layers, frames, cels, palette, or an existing loop', 'The eyedropper options bar offers “Replace in current layer” and “Replace globally”, replacing foreground by background directly', 'Hold Alt+S and click to sample, which adds the resulting foreground color to the palette'] },
          { kind: 'h3', id: 'colors-modes', text: 'The three color modes' },
          { kind: 'ul', items: ['RGBA: edit color and transparency directly, best for freehand work and semi-transparent effects', 'Indexed: pixels reference palette colors, best for palette management and limited-palette pieces', 'Grayscale: paint in grey levels, best for value studies and grey assets'] },
          { kind: 'p', text: 'Convert through “Image > Color mode”. Conversion changes the project color data; when you only want to check values, use the relative luminance view instead.' },
        ],
      },
      {
        id: 'selection',
        title: 'Selection, move, and transform',
        blocks: [
          { kind: 'p', text: 'Selections confine an edit to part of the canvas, and moving and transforming complete the precise local edit.' },
          { kind: 'h3', id: 'selection-tools', text: 'Building and combining selections' },
          { kind: 'ul', items: ['Rectangle M and ellipse Shift+M: drag to create, Ctrl to create from the center, Shift to constrain the ratio while creating; rectangles support rounded corners', 'Lasso Q: drag to trace any outline and release to close; polygon lasso Shift+Q clicks point by point and finishes with a double click, a click on the start point, or Enter, with Esc to cancel', 'Magic wand W: samples by color with tolerance, contiguity, and smart close', 'Fixed-center drawing: the options bar for rectangle and ellipse selections and their shapes offers the switch and a drawing anchor. The anchor defaults to the canvas center and can be set with a nine-grid preset, X/Y coordinates, or by dragging it on the canvas, with its own visibility toggle; when enabled, shapes are created outwards from that fixed point, and when disabled you are back to the default press position or Ctrl for a temporary center. The anchor and switch are tool preferences and never enter artwork undo', 'The options bar offers new, add, subtract, and intersect; hold Shift before starting to add temporarily, and the right button subtracts by default', 'Ctrl+A selects all, Ctrl+D deselects, Ctrl+Shift+I inverts, and Ctrl+H hides only the marching ants while the selection still limits painting', 'Double-clicking with the rectangle marquee selects the current grid cell; pressing again and dragging continues through consecutive cells', 'In the timeline, Alt+click a cel to select its non-transparent content and Shift+Alt to add it to the current selection'] },
          { kind: 'h3', id: 'selection-move', text: 'Moving and copying' },
          { kind: 'ul', items: ['Drag inside the selection to move pixels, or drag the marquee edge to move the marquee alone', 'Ctrl+drag copies the selection content, and Shift constrains the direction to horizontal or vertical', 'Content stays floating until you confirm: Enter confirms and Esc cancels the current transform', 'Move tool V moves the current frame of a layer, and “Auto-select layer” hits layers by canvas content', 'Shift+click adds cels to the selection, dragging the selected content moves them together, and Alt+drag copies a layer', 'Ctrl+C, Ctrl+X, and Ctrl+V copy, cut, and paste; Ctrl+Shift+V pastes as a new layer, and “Edit > Paste special > Paste as new project” creates a separate project', 'Deleting selected content clears pixels, not just the marquee'] },
          { kind: 'h3', id: 'selection-transform', text: 'Scale, rotate, and flip' },
          { kind: 'ul', items: ['Ctrl+T enters content transform: drag handles to scale, drag in the rotation area to rotate, and skew depending on where you interact', 'While scaling, Shift keeps the ratio, Ctrl snaps to integer multiples, and Alt transforms on both sides of the pivot; while rotating, Shift snaps to 45°', '“Select > Free transform” is a separate four-corner proportional scale entry that keeps only the corners and does not rotate or skew', 'Horizontal and vertical flips under “Select > Transform” really flip the content, on Shift+H and Shift+V by default', 'Selecting several normal layers or groups and then several frames transforms that combination while preserving each layer structure', 'Combining text, tiles, free tiles, hidden, or locked targets blocks the combined transform'] },
          { kind: 'h3', id: 'selection-effects', text: 'Outline, antialiasing, and conversions' },
          { kind: 'ul', items: ['“Select > Outline” (Shift+O) outlines the content inside a selection, or the non-transparent content of the current layer when there is none; set color, width, inside/outside position, shape, and pixel direction, with smart hue deriving the outline from source colors', 'The difference from layer styles is that this produces real pixel edits', '“Edit > Auto antialiasing” generates transition pixels from the outline, with automatic color picking, opacity, canvas or palette colors, and an option to process internal color boundaries', 'Auto antialiasing previews first, restores the original pixels if you cancel or close, and undoes or redoes in one step once applied — including Aseprite projects with offset or cropped cels', '“Select > Convert to” creates pattern brushes or background presets; a background preset saves the current visible composite and stays transparent outside a non-rectangular selection'] },
        ],
      },
      {
        id: 'layers',
        title: 'Layers, groups, and masks',
        blocks: [
          { kind: 'p', text: 'A layer is a track running across the animation, a frame is one full column, and a cel is one layer’s content in one frame.' },
          { kind: 'h3', id: 'layers-manage', text: 'Managing layers' },
          { kind: 'ul', items: ['Create with “Layer > New layer” or Shift+N; click to select, Ctrl to add or remove, and Shift to select consecutively in display order', 'Drag to reorder, drag into a group to nest, and Alt+drag to duplicate', 'The eye controls visibility and the lock controls editing; hold and drag across rows to set several at once, and Alt+click applies to objects at the same level', 'Double-click or use the context menu to open properties and change name, opacity, blend mode, display color, and description', 'A display color only labels the layer in the list and never changes artwork pixels'] },
          { kind: 'h3', id: 'layers-mask', text: 'Per-frame masks' },
          { kind: 'p', text: 'Right-click a cel to create a layer mask, or right-click a layer or group to create masks for the frames that already have content. Black hides, white shows, grey partially shows, and colored strokes are converted to grey; erasing restores the “not painted” state, where unpainted areas behave as white and the original artwork still shows.' },
          { kind: 'ul', items: ['After creating, find the mask track above the object and click the mask cell for a frame to paint', 'Alt+click a mask cell for an isolated white-on-grey mask view; click a normal layer or cel, or switch frames, to leave it', 'Masks are stored per frame and can be multi-selected, copied, pasted, dragged, linked, and unlinked', 'A group mask applies to the composite result of the whole group', 'A mask is not a black-and-white painting on a normal layer: it never overwrites the original pixels'] },
          { kind: 'h3', id: 'layers-styles', text: 'Layer styles' },
          { kind: 'p', text: 'Right-click a layer or group > Layer styles, then choose and enable effects, tune parameters against the live preview, apply to commit, or cancel to restore the previous image.' },
          { kind: 'ul', items: ['Outline: width, inside/outside/both, contour shape, and eight-direction switches; “smart hue” derives the outline from neighbouring source colors', 'Shadow: “smart shadow” derives its dark color from the background below it and updates when that background changes', 'Inner glow, color overlay, and gradient overlay (smooth, Bayer, or directional dither)', 'For smart outline or shadow, a 0% darkening factor keeps the source color and 100% turns it black', 'Style settings are shared across frames, but each frame computes the effect from its own content, and disabling a style keeps its parameters', 'To turn an effect into pixels you can keep painting on, use “Convert to > Normal layer”'] },
          { kind: 'h3', id: 'layers-group', text: 'Groups and clipping masks' },
          { kind: 'ul', items: ['Ctrl+G groups the selection, and “Layer > New group” does the same; groups nest and can be moved, copied, hidden, locked, or given styles as a unit', 'Ctrl+Shift+G ungroups while keeping the members', 'Right-click a layer or group to enable a clipping mask, which limits visibility to the visible shape of the object directly below at the same level — ideal for shadows and highlights inside a base silhouette', 'A normal, fully opaque group composites in pass-through mode; changing group opacity or blend mode makes the group composite as a whole', '“Accumulate blending” offers another way to blend a group with what is beneath it'] },
          { kind: 'h3', id: 'layers-merge', text: 'Copy, delete, and merge' },
          { kind: 'ul', items: ['Duplicating a layer includes all its frames, deleting removes the whole track, and copying layers or groups between projects keeps the hierarchy, position, and properties that can be supported', 'What Delete removes depends on the last active area, so confirm focus is in the layer panel first', 'Merge down: composites the current layer with the one directly below, keeping the merged result per frame in the current build', 'Merge selected layers: merges selected layers that are at the same level, consecutive, visible, and unlocked, producing a per-frame result', 'Merge group and Merge visible: the current implementation still produces a result for the active frame — for multi-frame animation use the two per-frame commands above'] },
          { kind: 'h3', id: 'layers-linked', text: 'Linked layers' },
          { kind: 'p', text: 'Right-click a normal pixel layer > Create linked layer. The two layers then share pixels in their corresponding frames: drawing, erasing, or recoloring any member updates the others, while position, name, level, visibility, lock, opacity, blend mode, and styles stay independent.' },
          { kind: 'ul', items: ['Useful for repeated decoration, side-by-side comparison, or giving the same content different display treatments', 'Duplicating a linked member inside the project keeps it in the original group; pasting across projects creates a new group independent of the source', 'Background, text, tilemap, and free tile layers cannot create this kind of link directly', '“Linked layers” connects different layers, while “linked cels” connects frames of the same layer — keep the two apart'] },
          { kind: 'h3', id: 'layers-background', text: 'Background layers and type conversion' },
          { kind: 'ul', items: ['Right-click “New > New background layer” for solid color, grid, stripes, diamonds, circles, or an existing preset', 'The background sits at the bottom of the root and carries an underscore in its name, and it can still be painted, hidden, and exported', 'A normal pixel layer can be converted to a background, and enlarging the canvas extends it by its existing repeat unit', 'Sampling from a normal layer ignores the background so foreground transparency is preserved; select the background layer to sample its composited color instead', '“Convert to” offers normal layer, background layer, or tile layer; converting to a normal layer bakes text, tiles, or enabled styles into pixels', 'Conversion is undoable, but once saved as a plain image those project structures cannot be restored'] },
        ],
      },
      {
        id: 'animation',
        title: 'Animation, cels, and loops',
        blocks: [
          { kind: 'p', text: 'Animation controls and frame cells live in the layer panel. If no frame column is visible, turn off “Hide timeline” in the layer settings — while hidden, playback and related actions are disabled but frame data is kept.' },
          { kind: 'h3', id: 'animation-frames', text: 'Create, order, and play' },
          { kind: 'ul', items: ['The add-frame button duplicates the current frame by default, producing an independently editable copy; right-click a frame header to add a blank frame, duplicate, or delete, with blank frames defaulting to 100 ms', 'Double-click a frame header to change its duration; multi-selected frames can be processed in bulk or dragged into a new order, and at least one frame is always kept', 'Press Enter to play or pause; comma and period step to the previous and next frame, and the left and right arrows also work when there is no selection', 'During playback the first press of a step key stops it, and the next press steps', 'Right-click the play button for play once, loop all frames, or play tag and repeat, plus a 0.25x–3x speed and returning to the start frame on stop', 'Speed affects preview only and never rewrites the duration stored per frame', 'Disabling a frame keeps its content editable while normal playback skips it, and frame commands can re-enable it'] },
          { kind: 'h3', id: 'animation-cel', text: 'Selecting, copying, and linking cels' },
          { kind: 'ul', items: ['Select one or more cels in the frame grid, then copy, paste, or delete from the context menu, or drag them to a target cell', 'Copying a frame covers every layer of that frame, while copying cels touches only the selected cells', 'Double-clicking a cel that has content sets the cell opacity; double-clicking a text cel enters text editing', 'Linked cels share content, so editing one member updates the group; a plain copy produces an independent duplicate, which is what you want for frame-by-frame changes', 'The link icon on layer and mask tracks can enable automatic linking so newly created frames continue the linked content; turning the switch off does not break existing links'] },
          { kind: 'h3', id: 'animation-tween', text: 'Automatic tweening' },
          { kind: 'p', text: 'Right-click a timeline frame header or cel > Generate tween frames: pick a normal pixel layer with visible, unlocked content and a starting frame, choose “single frame” or “loop” as the source, then set how many frames to add, the duration per frame, end-point horizontal and vertical offset, rotation, scale, and relative opacity, choose constant, ease-in, ease-out, or ease-in-out, and generate.' },
          { kind: 'ul', items: ['With a single-frame source, other layers follow the starting content; with a loop source the app cycles the source frames, skips disabled frames, and inserts the new frames after the source loop', '“Frames to add” includes the end frame, and rotation and scaling happen around the content center', 'The preview does not include masks, layer styles, or blend effects', 'Text, tile, and background layers must be converted to normal layers first', 'It generates geometry and opacity transitions — it does not redraw a character’s in-between poses'] },
          { kind: 'h3', id: 'animation-onion', text: 'Onion skin and loops' },
          { kind: 'ul', items: ['Enable onion skin in the layer panel to overlay neighbouring frames in a tint as motion reference; set the range, opacity, previous and next frame colors, and whether it shows during playback', 'Onion skin is for viewing only and never appears in the final image', 'Select consecutive frames and right-click a frame header to create a named loop, such as “idle” or “walk”; double-click the bracket to edit name, start and end frames, direction, and repeat count, or drag the bracket edges to change the range', '“Play loop” on the context menu plays it by its repeat count, and “Play tag and repeat” on the play button keeps looping the innermost loop containing the current frame', 'Adding frames inside a loop extends its range, and a loop can serve as the source range for GIF, sprite sheet, or tween exports'] },
        ],
      },
      {
        id: 'tiles',
        title: 'Tilemap and free tiles',
        blocks: [
          { kind: 'p', text: 'Tile layers organize a scene with a shared tile set: one tile can be referenced in many places, and editing the source updates every reference.' },
          { kind: 'h3', id: 'tiles-create', text: 'Creating layers and sharing tile sets' },
          { kind: 'ul', items: ['Right-click a layer > New > New tile layer, then enter a name and the tile width and height', 'Choosing an existing tile set keeps its dimensions and lets several layers share it; choosing a new one creates a fresh set', 'A normal or background layer can be split across all frames with “Convert to > Tile layer”: identical blocks are reused, an incomplete edge block is padded with transparency, and content outside the canvas is not kept', 'Text or styled content should be converted to a normal layer first'] },
          { kind: 'h3', id: 'tiles-modes', text: 'Four editing modes' },
          { kind: 'ul', items: ['In-place edit: change an existing tile with pixel tools, and every reference to that tile updates', 'Variant create: change the pattern of the current cell, creating or reusing a variant and replacing only that cell’s reference', 'Mixed edit: edit inside existing cells and paint new content into empty ones; existing tile edits propagate while empty-cell results create or reuse tiles', 'Draw tiles: pick blocks from the tile set and lay them down cell by cell, changing cell references without touching the tile source'] },
          { kind: 'p', text: 'New layers start in mixed edit. Clicking an actual tile in the tile set enters draw-tiles mode: the left button picks the foreground tile and the right button the background tile, and on the canvas the left and right buttons lay them down respectively. In that mode the eyedropper picks the tile reference and the eraser clears a cell.' },
          { kind: 'h3', id: 'tiles-manage', text: 'Organizing and moving' },
          { kind: 'ul', items: ['Tile sets support multi-selection, deletion, dragging slots into a new order, and Ctrl+scroll to change display size; reordering never changes canvas references', 'Hold Ctrl to see tile numbers, and at least one tile is always kept', 'In draw-tiles mode a selection expands to whole cells and moves snap to the tile width and height', 'Moving a whole layer changes only the position of the current cel — not the tile source or the grid', 'One tile set can be referenced by several layers, so confirm the mode before editing, or a single-variant edit can change every reference'] },
          { kind: 'h3', id: 'tiles-free', text: 'Free tiles: draw once, reuse everywhere' },
          { kind: 'p', text: 'Free tiles suit repeated elements such as trees, rocks, character parts, and decoration: instances do not need to align to a fixed grid, they may overlap, and editing the source updates every reference.' },
          { kind: 'ul', items: ['Right-click a layer > New > New free tile layer, then enter a name and choose a new or existing set', 'A new set prepares a transparent source tile and enters “Edit source tile”: draw on the canvas directly, and the first stroke creates an instance automatically', 'Pick a source in the tile set panel, switch to “Place instance”, then click or drag on the canvas to place it', 'Select an instance in the instance list and use the move tool to reposition it; instance properties cover X, Y, rotation, mirror, opacity, and blend mode', 'To change every copy of the same object, select one instance and edit the shared source with the pencil, eraser, or selection tools', 'For another pattern, click “Add source tile”, draw the new source, and place it'] },
          { kind: 'p', text: 'A source holds the pattern; an instance holds where that pattern sits and how it is oriented. The instance list supports Ctrl to add or remove, Shift for consecutive selection, drag reordering, visibility, locking, bulk properties, and bulk deletion, and it can be shown on its own or returned to the layer panel. In place-instance mode the eraser removes the topmost instance under the pointer and the eyedropper selects its source; that mode does not use marquee selection or Ctrl+T.' },
        ],
      },
      {
        id: 'image',
        title: 'Image adjustments, filters, and size',
        blocks: [
          { kind: 'p', text: 'Adjustment commands show a live preview and produce a single undoable change once confirmed. With a selection they stay inside the current layer’s selection; without one they process the selected layers.' },
          { kind: 'h3', id: 'image-adjust', text: 'Color adjustments' },
          { kind: 'ul', items: ['Color balance: adjust cyan/red, magenta/green, and yellow/blue separately in shadows, midtones, and highlights, with an option to preserve luminosity', 'Brightness/contrast: change overall lightness and the distance between light and dark', 'Hue/saturation Ctrl+U: change hue, color intensity, and HSL lightness, where the ends of lightness tend toward black and white', 'Curves Ctrl+M: click to add control points and drag to reshape the tonal relationship, with per-channel editing and removal of intermediate points'] },
          { kind: 'h3', id: 'image-filters', text: 'Display-style filters' },
          { kind: 'ul', items: ['“Edit > Filters” offers classic CRT interlacing, RGB phosphor grid, interlaced grid, VHS color shift, vignette, and green phosphor glow', 'Presets produce effect layers whose strength can be tuned through layer visibility, opacity, or blend mode', 'The LCD screen filter needs a selected layer plus channel offset width and height and a scanline type', 'Filters are a visual style treatment, not a canvas display zoom option, and can be undone from history'] },
          { kind: 'h3', id: 'image-size', text: 'Canvas size, image size, crop, and trim' },
          { kind: 'ul', items: ['Resize canvas C changes the working bounds with size, anchor, or per-side margins, and “Link all sides” makes the margins equal; only “Crop content outside the canvas” actually discards out-of-bounds content', 'Resize image Ctrl+Alt+I scales the whole artwork with width, height, and interpolation: nearest neighbour keeps pixel edges, bilinear smooths', 'Detect pixel scale identifies pixel art that was enlarged by an integer factor in the image-size window, then scales it back with nearest neighbour; flat colors or a lack of reliable features can prevent detection', 'Image > Crop sets the canvas to the current selection', 'Image > Trim removes transparent edges based on the current frame or all frames; for animation, trim by all frames so one frame’s bounds do not exclude the others'] },
        ],
      },
      {
        id: 'slices',
        title: 'Slices and asset splitting',
        blocks: [
          { kind: 'p', text: 'Slices name regions of the canvas and export icons, buttons, or other assets by region. They never cut the original layers apart.' },
          { kind: 'h3', id: 'slices-create', text: 'Creating and managing slices' },
          { kind: 'ul', items: ['Pick the slice tool Shift+C and drag out a region on the canvas', 'Select a slice and change its name, position, and size in the options bar or slice properties', 'Drag a slice or its edges to adjust the range, and manage slices with select-all and delete', 'For regular asset sheets, open “Auto slice”, set the slice size, starting position, and horizontal and vertical spacing, check the expected count, then create', '“Window > Show > Show slice borders” controls the guide borders'] },
          { kind: 'h3', id: 'slices-export', text: 'Exporting by slice' },
          { kind: 'p', text: 'When exporting, set the region to slices and then choose all slices or a specific one; sprite sheets can also use named slices as their region. Batch slice output does not currently use the animated GIF format.' },
        ],
      },
      {
        id: 'files',
        title: 'Saving and exporting',
        blocks: [
          { kind: 'p', text: 'Saving a project and exporting a deliverable are two different paths: the first keeps the editable structure, the second hands something to an engine, a compositor, or a client.' },
          { kind: 'h3', id: 'files-formats', text: 'Choosing a format' },
          { kind: 'ul', items: ['.moonsprite: the complete editable project; to carry local timelapse recordings to another computer, save as and tick the carry option', '.ase and .aseprite: exchange supported layers and animation with Aseprite, but do not keep them as the only master copy of MoonSprite-specific data', 'PNG (auto-indexed or RGBA), JPEG, WebP, BMP: still image output that does not keep project editing structure', 'ICO: Windows icon output; SVG: pixel art in a vector container, not automatic recognition of pixels into editable curve paths', 'GIF: frame animation, limited by what GIF can express in color and transparency', 'PSD: exchange the current frame’s layer structure with Photoshop; currently export-only with no PSD import, no animation timeline, and text and tiles written as raster'] },
          { kind: 'p', text: 'After opening an image directly, “Save in original format”, which is on by default, lets a normal save overwrite the source image. When you add layers, frames, text, masks, or other structures that format cannot keep, the app first lists the compatibility loss and lets you either save as .moonsprite or continue deliberately; cancelling writes nothing. With that preference off, a normal save of a non-native format asks whether to save as .moonsprite first. Save As always follows the format you choose.' },
          { kind: 'h3', id: 'files-export', text: 'Exporting stills, regions, and GIFs' },
          { kind: 'ul', items: ['“File > Export > Export as” (Ctrl+E) chooses format, file name, and directory', 'Choose the export region and the available layer range: canvas, frames, or slices', 'Set the scale and the trim mode; pixel display images usually use integer scales', 'For GIF, choose all frames, a frame range, or a loop, and set forward, reverse, or ping-pong direction', '“Trim individually” removes each target’s transparent edges, while “trim uniformly” gives the batch one shared boundary, which keeps animation aligned', 'Export presets store frequently used format, scale, range, and path settings, and a project remembers its last successful export', 'PSD supports only the full canvas of the current frame, keeping supported group hierarchy, visibility, transparency, blend modes, clipping, masks, and mappable styles'] },
          { kind: 'h3', id: 'files-frames', text: 'Exporting all frames and sprite sheets' },
          { kind: 'ul', items: ['“Export all frames” writes consecutively numbered images in timeline order, reusing the format, scale, and directory settings — good for engine import or frame-by-frame delivery', '“Export sprite sheet” sets the layout: horizontal strip, vertical strip, by rows, or by columns, with optional limits on columns, rows, total width, or total height', 'The sheet source can be the canvas or slices, a layer range, and all, selected, or loop frames, splitting layers or loops as needed', '“Merge duplicates” gives identical frames a single rectangle, and “Ignore empty slots” allocates no area to transparent frames', 'Split targets are arranged and appended into one sheet rather than written as several files', 'Enabling preview shows the layout; turning “Output file” off produces an unsaved single-layer project, while turning it on writes the PNG'] },
        ],
      },
      {
        id: 'workspace',
        title: 'Workspace, split view, and preview',
        blocks: [
          { kind: 'p', text: 'The interface serves the artwork: panels dock where you want them, and the layout itself is part of a workspace.' },
          { kind: 'h3', id: 'workspace-panels', text: 'Panels and workspaces' },
          { kind: 'ul', items: ['“Window > Panels” shows color, palette, layers, history, pattern brushes, tile set, and preview', 'Right-click a panel title to hide it, dock it left, right, or bottom, or float it; dragging titles and dividers also rearranges the layout', 'The workspace manager saves the current layout, switches between layouts, and resets; layout changes save automatically, and the default workspace cannot be deleted', 'Number keys 1–6 call up color, palette, layers, preview, tile set, and pattern brushes temporarily; clicking outside, pressing Esc, or triggering again puts them back without changing the docked layout', 'The toolbar can sit on any of the four sides via “Window > Toolbar position”, and the quick command bar is configured in preferences'] },
          { kind: 'h3', id: 'workspace-split', text: 'Split view and floating project windows' },
          { kind: 'p', text: 'Drag a project tab into the top, bottom, left, or right area of the canvas and release when the dock hint appears to create a split, then drag the divider to change the ratio; clicking a canvas makes its project the current editing target. Right-clicking a project tab floats the view as a project window, and a floating window can return to the tab bar. Splitting, floating, and returning only change where a view is shown — they never copy files on disk.' },
          { kind: 'h3', id: 'workspace-preview', text: 'Preview and history' },
          { kind: 'ul', items: ['The preview panel zooms independently, which suits fixing pixels up close while watching the whole image; “Follow canvas viewport” tracks the main canvas position, and dragging the preview manually leaves that mode', 'The preview play button checks the animation on its own', 'The history panel lists reachable editing steps, and clicking one undoes or redoes in order to that state; painting after stepping back clears the later branch', 'Zoom, pan, mirror, and layout are not artwork changes and never appear in history'] },
        ],
      },
      {
        id: 'prefs',
        title: 'Preferences, themes, and shortcuts',
        blocks: [
          { kind: 'p', text: 'Open preferences from “Edit > Preferences”. “Apply” saves and keeps the window open, “OK” saves and closes, and “Cancel” discards changes that were not applied. Category names may be reorganized between versions.' },
          { kind: 'h3', id: 'prefs-groups', text: 'Common settings' },
          { kind: 'ul', items: ['General and appearance: language, interface scale, and common display preferences; interface scale is independent from canvas zoom', 'Input and editing: wheel and zoom behavior, view drag sensitivity, line preview and step algorithm, lasso closing preview, and whether sampling returns to the pencil', 'Tablet and touch: tablet interface, pen pressure, eraser end, side buttons, canvas right-button action, and one- and two-finger gestures', 'Tools and snapping: grid snapping, smart snapping, guides, and thresholds', 'Cursor and preview: system or MoonSprite pointer, pointer size, brush edge and full preview, edge during drawing, marquee crosshair, and size hints', 'Background and guide colors: transparency checkerboard, grid, onion skin, symmetry axis, and text box colors', 'Color and layers: color picking mode, layer display color presets, and related editing preferences', 'Files and recovery: default save format and directory, default export directory, recovery interval and retention days, and timelapse defaults', 'Undo, presets, quick command bar, extensions and scripts, plus a reset entry that restores initial settings'] },
          { kind: 'p', text: 'Number fields accept simple arithmetic such as 32*3 or 128/2, evaluated on Enter or when focus leaves the field, and Tab and Shift+Tab move between fields in the same form. Resetting clears local configuration but never deletes user projects or resource files.' },
          { kind: 'h3', id: 'prefs-theme', text: 'Themes' },
          { kind: 'p', text: 'Choose an existing theme in theme settings, or create and duplicate your own. Editing the base colors changes the interface palette, while guides such as the grid, checkerboard, and onion skin can be overridden individually or set back to “follow theme”. Built-in themes are read-only, so editing creates a custom copy; theme configuration can be imported and exported for backup and sharing. The dark and light switch on this website comes from the app’s Dark and Light theme palettes.' },
          { kind: 'h3', id: 'prefs-keys', text: 'Shortcuts' },
          { kind: 'ul', items: ['“Edit > Shortcut settings” finds commands by category and records one or more combinations, including keyboard, middle and side mouse buttons, and the wheel', 'Every tool also has a “quick select” entry that switches temporarily while held and restores on release', 'Conflicts are reported so you can see what is occupied, and keys shared between tools can cycle through them', 'The whole draft is committed only when you click done, and cancelling saves nothing; shortcut configuration imports and exports as JSON', 'Canvas wheel shortcuts fire only inside the canvas, and editing keys inside a text field keep their normal behavior'] },
        ],
      },
      {
        id: 'extensions',
        title: 'Extensions and the runtime',
        blocks: [
          { kind: 'p', text: '.msext is a ZIP-container extension package: the archive root must hold manifest.json, and every entry point and resource lives inside the package. An extension cannot rely on absolute install paths or import MoonSprite’s React, Store, or Tauri modules. The repository’s extension development and Extension Runtime v1 documents are the reference for every field and call.' },
          { kind: 'h3', id: 'ext-models', text: 'Two execution models' },
          { kind: 'ul', items: ['schemaVersion: 1 is a declarative Lua extension, suited to undoable document commands, menus, and command panels', 'schemaVersion: 2 adds Extension Runtime v1, host-component settings, and extension-owned windows on top of those Lua commands; declaring runtime also requires apiVersion: "1.0.0"', 'commands[], panels[], menuItems[], and topMenus[] work in both versions; settingsUi and runtime belong to schema 2 only', 'settingsEntry is a restricted HTML settings page available to both, used only when host components cannot express the interface'] },
          { kind: 'h3', id: 'ext-runtime', text: 'What a runtime extension can do' },
          { kind: 'ul', items: ['A resident sandbox HTML/JavaScript page: runtime.entry is a self-contained UTF-8 document running in a sandbox="allow-scripts" iframe with no same-origin access', 'Windows: windows.open creates a native frameless attached window, a host dialog, or an in-window overlay, with position, size, and hit regions declared by the extension; windows are owner-bound and never system-topmost', 'Menus and commands: menus.setItems replaces the options of the extension’s own top-level menu, and a menu click comes back as a command event', 'Projects and documents: read the project list, summary, layers, and frames; undo and redo; switch tools and set the primary and secondary colors — but there is no arbitrary pixel writing', 'Storage is isolated per extension ID: keys up to 160 bytes, 256 KiB per value, 1 MiB total; network calls go through the host, HTTP(S) only, 15-second timeout, 2 MiB per response body', 'Events: project, command, interaction, clock, document-saved, export-complete, settings-changed, window-message, and editor-event; events are hints, not a reliable queue, and missed ones are not replayed', 'Declared permissions do not grant abilities by themselves: call runtime.getCapabilities() at startup and use only the returned methods and windowPresentations'] },
          { kind: 'h3', id: 'ext-install', text: 'Installing and managing' },
          { kind: 'ul', items: ['Install .msext packages from extension management in “Preferences > Extensions”, drag a package into the app, or double-click it when file association is configured', 'Installation validates and extracts into a staging directory first, then atomically replaces the same extension ID, keeping the enabled state across updates', 'Only enabled extensions contribute a runtime, menus, or panels; once disabled, every entry point disappears and no path can force execution', 'Disable it to stop, or uninstall and open the extension folder from extension management; disabling or uninstalling destroys the runtime and closes the windows it created', 'There is no fixed top-level “Extensions” menu: entries come entirely from what extensions contribute, and a schemaVersion 2 runtime extension needs a host that implements Runtime v1'] },
          { kind: 'h3', id: 'ext-limits', text: 'Limits and boundaries' },
          { kind: 'ul', items: ['Archive limits: 50 MiB, 256 files, 256 MiB extracted; runtime entry 1 MiB and settings entry 512 KiB', 'At most 64 runtime resources of 16 MiB each, 64 commands, 16 panels, 32 built-in menu contributions, 16 top-level menus, and 64 settings controls', 'The Lua compatibility layer is declarative: it cannot inject React, DOM, CSS, JavaScript, or native controls, and cannot load packaged or system files with require', 'Any pixel or document-structure write still goes through a packaged Lua command so transactions, undo, and rollback are preserved', 'A schema 2 runtime cannot run on an older MoonSprite without Runtime v1, so extensions must probe capabilities instead of assuming a method exists'] },
          { kind: 'h3', id: 'ext-pet', text: 'The built-in Pet Companion' },
          { kind: 'ul', items: ['It ships as an ordinary .msext extension: disable it, uninstall it, or read it as a complete runtime extension example', 'Supports multiple pets, custom animations, condition-driven animation slots, and .mspet pet pack import and export', 'Exports trim transparent edges and keep integer pixel scaling plus hit testing on transparent areas', 'Pets can be dragged and remember their position, and offer a project info bubble along with clock, save, and break reminders'] },
        ],
      },
      {
        id: 'scripting',
        title: 'Lua scripts',
        blocks: [
          { kind: 'p', text: 'A plain Lua script is the light path: one file dropped into the scripts folder, good for one-off batch work and custom dialogs. When you need resident interface, menus, or windows, use an .msext extension instead — see the previous chapter. Both share the same Lua sandbox and transaction rules.' },
          { kind: 'h3', id: 'scripting-lua', text: 'Running Lua scripts' },
          { kind: 'ul', items: ['“File > Scripts > Open scripts folder”, place UTF-8 .lua files at the top level of that directory, then reopen the File menu and run them from the script list', 'Scripts can provide batch drawing, asset processing, or custom dialogs; they carry no interface and need no installation', 'Some Aseprite-style interfaces are supported alongside the dedicated mse.* API, but not every Aseprite script runs unchanged', 'Limits such as normal-pixel targets, layer types, and the current frame affect what a script can do; read the result message when it fails', 'Scripts have no direct access to arbitrary system files, the network, or processes; the bundled moon-phase.lua example is placed in that folder on first open, and an existing file of the same name is never overwritten'] },
          { kind: 'h3', id: 'scripting-ns', text: 'How app.* and mse.* divide the work' },
          { kind: 'ul', items: ['app.* is the Aseprite compatibility namespace for migrating existing scripts and implements only an explicit subset', 'mse.* is MoonSprite’s own API: it does not pretend to be another app’s API and does not expose the internal SpriteDocument or Renderer state', 'The compatibility subset has two layers — base pixels and layers, then animation reading and editing; creating, duplicating, or deleting frames and creating cels in empty frames are not open yet, so use the typed mse.* interfaces instead', 'Scripts should probe with mse.apiVersion, mse.status, mse.capabilities, and mse.isSupported(); every method in the capability table is a real implementation rather than a placeholder that only errors', 'Compatibility scripts run under the same image, memory, instruction, and execution-time budgets, so a per-pixel neighbourhood scan can stop on a large canvas'] },
          { kind: 'h3', id: 'scripting-transaction', text: 'Transactions and undo' },
          { kind: 'code', text: 'local layers = mse.layers.list()\n\napp.transaction("Create palette color", function()\n  mse.palette.create { color = { r = 41, g = 121, b = 255, a = 255 } }\n  mse.layers.update(layers[1].id, { name = "Lua Layer", opacity = 192 })\nend)' },
          { kind: 'p', text: 'Pixel edits and mse.* writes join the same transaction and form a single undo step; if any operation fails validation, the whole batch rolls back. Query methods return a structural snapshot taken when the script started, and modifying a copy never affects the project. Creating or opening another project, saving, exporting, importing local brushes, and showing or hiding panels are file and app operations that stay out of the current project’s undo history.' },
          { kind: 'h3', id: 'scripting-help', text: 'Help and diagnostics' },
          { kind: 'p', text: 'The Help menu offers the component library, changelog, usage statistics, diagnostic logs, and about. When something goes wrong, note the app version, the reproduction steps, and any message, then open the diagnostic log if needed. Usage statistics never affect your artwork.' },
        ],
      },
      {
        id: 'compatibility-api',
        title: 'Aseprite compatibility layer',
        blocks: [
          { kind: 'p', text: 'app.* implements only the subset the project has explicitly listed, so existing scripts can migrate — it is not full Aseprite compatibility. New scripts are better off with mse.*: the typed interfaces cover more, and none of the traps below apply. This page lists the members and limits as currently implemented; the repository’s Lua compatibility API document carries the full signatures.' },
          { kind: 'h3', id: 'compat-globals', text: 'Globals and app' },
          { kind: 'ul', items: ['Constructors: Point, Rectangle, Color, Image, Palette, Sprite, and Dialog; Sprite(width, height) stages a new document and the host only creates it when the result is applied', 'app.activeSprite / sprite / activeImage / activeLayer / activeCel / activeFrame / frame / range form the current script context, with frame numbers starting at 1', 'app.fgColor and app.bgColor are colour snapshots; assigning to an ordinary table on app never calls a host command', 'app.transaction([label,] fn) merges queued edits into one transaction and returns the callback result — it is the entry point for every write', 'app.useTool accepts only line and eraser, with points in document coordinates; any other tool name is rejected', 'app.alert() only appends text to the script result rather than opening a blocking native dialog, and app.refresh() with app.command.Undo/Redo() are compatibility placeholders that do nothing', 'app.pixelColor offers rgba / rgbaR·G·B·A and graya / grayaV·A / index, chosen to match the image colour mode'] },
          { kind: 'h3', id: 'compat-members', text: 'Which members exist' },
          { kind: 'ul', items: ['Points, rectangles, colours: Point.x/y are writable, Rectangle is read-only, and Color exposes writable red/green/blue/alpha plus HSV components', 'Images and palettes: Image supports getPixel / drawPixel / putPixel / clear / drawImage / clone in image-local coordinates and requires matching colour modes; Palette.getColorIndex returns the entry ID of the nearest colour, not an array index', 'Layers and cels: Layer exposes writable name, opacity, isVisible, isEditable, and isLocked, and Layer:cel(frame) fetches a cel; an existing cel is editable through its image and position', 'Frames and tags: Frame.duration is writable in seconds and converts to 1–60000 milliseconds on commit, unlike the millisecond durations in mse.*, and isDisabled is read-only; Tag.repeats = 0 means infinite, and aniDir treats only 1 as reverse', 'Selections: Selection offers just the read-only isEmpty, bounds, and contains — use mse.selection to change one', 'Sprite exposes read access to width, height, colorMode, palette, frames, layers, tags, and selection, and only its name is writable'] },
          { kind: 'h3', id: 'compat-gaps', text: 'Known gaps' },
          { kind: 'ul', items: ['There is no general way to create, duplicate, or delete frames; use the mse.* interfaces when you need to, or design the script around existing frames and cels', 'Sprite:newCel only works on layers the script created itself (including a staged Sprite): it cannot take an existing host layer, and it cannot create a cel in an empty frame', 'The isContinuous, stackIndex, and parent setters on Layer only change the Lua wrapper and never queue a host write; parent is a group ID string or nil, not a Layer object', 'There is no require, no arbitrary file, network, or process access, and no complete Aseprite command registry', 'ColorMode lists TILEMAP, but a constant existing does not mean tile images can be written'] },
          { kind: 'h3', id: 'compat-dialog', text: 'Dialog controls' },
          { kind: 'ul', items: ['Dialog { title?, onclose? } supports button, check, color, combobox, entry, label, number, radio, separator, and slider', 'Common options are id, label, text, enabled (true by default), and visible (true by default); callbacks are onclick, onchange, and onrelease', 'Values: check and radio use selected, color uses color, combobox uses options with option, and number uses value (numeric text is also parsed) with min, max, step, and decimals', 'dlg.data reads the current value by control ID, dlg:modify { id, ... } changes a control, and dlg:close() dismisses the dialog', 'dlg:show { wait = false } keeps callbacks alive after the first run completes; omitting wait uses the blocking flow', 'newrow() and repaint() do nothing and bounds returns a zero rectangle instead of real geometry; this is not the same protocol as the runtime host form or manifest settings'] },
          { kind: 'h3', id: 'compat-budget', text: 'Sandbox budgets' },
          { kind: 'p', text: 'A 1 MiB script, 4,194,304 image and modified pixels, 64 MiB of Lua memory, 64 KiB of output, 20,000,000 instructions, and a 2-second execution budget. These are safety limits rather than performance promises: a per-pixel scan over a large canvas can stop on an exhausted instruction or time budget, and that is not a syntax error.' },
        ],
      },
      {
        id: 'mse-api',
        title: 'MSE API reference',
        blocks: [
          { kind: 'p', text: 'MSE (MoonSprite Extension) is MoonSprite’s own Lua API, currently version 0.2.0 and in the experimental stage. New scripts should prefer it: the typed interfaces cover more and bring none of the compatibility layer’s traps. See the previous chapter when migrating an existing Aseprite script. This page lists the common endpoints and conventions per module; full type hints live in mse-api.lua in the repository and can be added to a VS Code LuaLS workspace library path.' },
          { kind: 'h3', id: 'mse-model', text: 'Execution model' },
          { kind: 'ul', items: ['Query methods return a structural snapshot taken when the script started; returned tables are copies, and modifying a copy never affects the project', 'Write methods return true after parameter validation and join the current script transaction', 'Pixel edits and mse writes in one transaction form a single undo step; if any operation fails, the whole batch rolls back', 'Creating or opening a project, saving, exporting, importing local brushes, and panel layout are outside the document and do not enter undo history', 'Colors use { r, g, b, a } with channels from 0 to 255; layer opacity uses the same range', 'IDs are case-sensitive, and frame arguments accept a stable frame ID or a 1-based frame number'] },
          { kind: 'h3', id: 'mse-capabilities', text: 'Capability discovery' },
          { kind: 'code', text: 'print(mse.apiVersion)                       -- "0.2.0"\nprint(mse.status.stage)                     -- "experimental"\nprint(mse.capabilities.layers.status)       -- "stable"\nprint(mse.isSupported("tiles.createLayer")) -- true' },
          { kind: 'p', text: 'Each module in mse.capabilities carries status (currently stable), readOnly, and methods (name, implemented, readOnly). Every method in the current capability table is a real implementation — there are no placeholder endpoints that only raise errors.' },
          { kind: 'h3', id: 'mse-document', text: 'mse.document' },
          { kind: 'ul', items: ['info(): returns a summary of the current document and active layer (id, name, filePath, width, height, colorMode, frame, activeLayer)', 'activeLayer(): returns the active layer structure, including type, position, size, opacity, blend mode, visibility, lock, and styles', 'create(spec): creates a project with name, width, height, and colorMode (rgba / grayscale / indexed)', 'open(path?): opens a file by path, or shows a file picker when omitted', 'save(spec?): saves the active project; { saveAs = true } performs Save As'] },
          { kind: 'code', text: 'mse.document.create {\n  name = "Sprite",\n  width = 64,\n  height = 64,\n  colorMode = "rgba"\n}' },
          { kind: 'h3', id: 'mse-layers', text: 'mse.layers' },
          { kind: 'ul', items: ['list() / get(id): return structure for normal, text, tile, and free tile layers', 'create(spec?): creates an empty normal layer with optional name and opacity', 'duplicate(id) / remove(id): duplicate or delete a layer, honouring cels, shared resources, and lock checks', 'update(id, patch): updates name, opacity, blendMode, visible, locked, x, y, description, displayColor'] },
          { kind: 'code', text: 'local layers = mse.layers.list()\napp.transaction("Rename layer", function()\n  mse.layers.update(layers[1].id, { name = "Outline", opacity = 192 })\nend)' },
          { kind: 'h3', id: 'mse-animation', text: 'mse.animation' },
          { kind: 'ul', items: ['frames(): returns { id, number, duration, active }[]', 'setFrame(frame): switches the active frame', 'loops() / createLoop(spec) / updateLoop(id, spec) / removeLoop(id): manage animation loops', 'play(id?): plays the given loop by ID, or the whole animation when omitted'] },
          { kind: 'p', text: 'Loop parameters: name, start (or startFrameId), ["end"] (or endFrameId), direction (forward / reverse), and repeatCount (nil means repeat forever).' },
          { kind: 'h3', id: 'mse-palette', text: 'mse.palette' },
          { kind: 'ul', items: ['list() / get(id): query palette colors', 'create(spec): { color = color }, or pass a color table directly', 'update(id, patch): update color or name', 'remove(id): delete a color', 'extract(spec?): extract colors from the current composite with limit from 1 to 4096 and mode replace or append'] },
          { kind: 'h3', id: 'mse-tiles', text: 'mse.tiles and mse.freeTiles' },
          { kind: 'ul', items: ['tiles.listSets() / getSet(id): return tile set name, grid size, layout, and stable tile IDs, plus packed RGBA pixels within budget', 'tiles.createSet(spec): create a tile set such as { name = "Terrain", tileWidth = 16, tileHeight = 16 }', 'tiles.createLayer(spec): create a tile layer, omitting tilesetId to make a new set', 'tiles.place(spec): place or clear a tile in a given cell of a tilemap cel, with rotation and flipping', 'tiles.edit(spec): replace the RGBA pixels of one tile', 'freeTiles.listSources() / createLayer(spec) / createSource(spec) / place(spec) / edit(spec): manage free tile layers and shared sources, where editing a source updates every instance'] },
          { kind: 'h3', id: 'mse-brushes', text: 'mse.brushes' },
          { kind: 'ul', items: ['list() / get(id): return project and local brushes, including coverage and colors within budget', 'importImage(): open an image picker and import a local pattern brush', 'createFromSelection(): create a project brush from the current selection', 'remove(id): delete a project brush or a listed local brush', 'Scripts cannot read arbitrary brush file paths or raw file bytes'] },
          { kind: 'h3', id: 'mse-selection', text: 'mse.selection' },
          { kind: 'ul', items: ['info(): returns exists, empty, hasMask, selectedPixels, and bounds', 'set(spec): sets a rectangular selection with an optional width * height mask of 0-255 values', 'clear() / invert(): clear or invert', 'transform(spec): supports dx, dy, flipHorizontal, flipVertical, reusing the existing selection move and flip history'] },
          { kind: 'code', text: 'mse.selection.set { x = 4, y = 8, width = 16, height = 12 }' },
          { kind: 'h3', id: 'mse-slices-styles', text: 'mse.slices and mse.styles' },
          { kind: 'ul', items: ['slices: list() / get(id) / create(spec) / update(id, patch) / remove(id) manage named export slices', 'styles.get(layerId?): query the styles of a given or the active layer', 'styles.apply(layerId, styles): apply a full style model; invalid sizes, directions, colors, or enums are normalized or rejected', 'styles.copy(layerId) / paste(layerId): use the app-shared layer style clipboard', 'styles.clear(layerId) / setEnabled { id, enabled }: clear or toggle styles'] },
          { kind: 'h3', id: 'mse-workspace-io', text: 'mse.workspace and mse.io' },
          { kind: 'ul', items: ['workspace.listPanels() / getPanel(id): query a panel’s visible and dock state', 'workspace.showPanel(id) / hidePanel(id) / setPanel { id, visible?, dock? }: dock is left, right, bottom, or floating, and panel layout never enters document history', 'io.open(path?) / io.save(spec?): equivalent to the document methods of the same name', 'io.export(spec): uses the app’s existing export model with name, format, directory, target, scale, and GIF parameters'] },
          { kind: 'h3', id: 'mse-ui', text: 'mse.ui' },
          { kind: 'ul', items: ['notify(text): show a message in the app status area', 'alert(value): compatible with app.alert, with the text going into the script result message', 'dialog(options): create a component-based dialog with existing callbacks and persistent sessions'] },
          { kind: 'code', text: 'local dlg = mse.ui.dialog { title = "Options" }\ndlg:number { id = "amount", label = "Amount", value = 4 }\ndlg:button { text = "Apply", onclick = function()\n  mse.ui.notify("Applied: " .. dlg.data.amount)\nend }\ndlg:show { wait = false }' },
          { kind: 'h3', id: 'mse-safety', text: 'Safety boundary' },
          { kind: 'ul', items: ['Lua cannot directly reach the file system, the network, processes, the debug library, the DOM, React, the raw store, the history stack, or arbitrary Tauri commands', 'File picking, saving, exporting, and resource import only go through the controlled entries this API opens explicitly', 'Pixel and structural writes are protected by target identity, revision, size, memory, instruction, execution-time, and undo validation'] },
        ],
      },
      {
        id: 'shortcuts',
        title: 'Default shortcut reference',
        blocks: [
          { kind: 'p', text: 'These are the defaults. If you have changed shortcuts, the app’s own “Edit > Shortcut settings” is authoritative; every tool also has a hold-to-switch “quick select” key.' },
          { kind: 'h3', id: 'shortcuts-file', text: 'File and editing' },
          { kind: 'ul', items: ['New Ctrl+N / Open Ctrl+O / Save Ctrl+S / Save as Ctrl+Shift+S', 'Export Ctrl+E / Close current project Ctrl+W', 'Undo Ctrl+Z / Redo Ctrl+Shift+Z', 'Copy Ctrl+C / Cut Ctrl+X / Paste Ctrl+V / Paste as new layer Ctrl+Shift+V', 'Hue-saturation Ctrl+U / Curves Ctrl+M / Replace color Ctrl+Shift+K'] },
          { kind: 'h3', id: 'shortcuts-tools', text: 'Tools' },
          { kind: 'ul', items: ['Pencil B / Eraser E / Airbrush J / Liquify Y', 'Rectangle marquee M / Ellipse marquee Shift+M / Lasso Q / Polygon lasso Shift+Q', 'Magic wand W / Move V / Shape U / Text T', 'Fill G / Gradient Shift+G / Eyedropper I / Swap colors X', 'Hand H / Zoom Z / Rotate view R / Temporary hand Space / Temporary eyedropper Alt', 'Slice Shift+C / Canvas size C / Image size Ctrl+Alt+I / Decrease and increase brush [ and ]'] },
          { kind: 'h3', id: 'shortcuts-selection', text: 'Selection, layers, and animation' },
          { kind: 'ul', items: ['Select all Ctrl+A / Deselect Ctrl+D / Invert Ctrl+Shift+I / Hide marching ants Ctrl+H', 'Transform Ctrl+T / Outline Shift+O / Flip content horizontally Shift+H / Flip content vertically Shift+V', 'New layer Shift+N / New group Ctrl+G / Ungroup Ctrl+Shift+G / Pattern brush from selection Ctrl+B', 'Play or pause Enter / Previous frame , / Next frame .', 'Fill with foreground color F / Relative luminance Ctrl+Y (note: redo is Ctrl+Shift+Z)', 'Show or hide the layer panel Tab / Temporary panels 1–6 / Full screen F11 / Advanced mode Ctrl+F'] },
        ],
      },
      {
        id: 'support',
        title: 'Support',
        blocks: [
          { kind: 'p', text: 'Issues and suggestions raised during development feed directly into the process.' },
          { kind: 'h3', id: 'support-issues', text: 'Report issues' },
          { kind: 'p', text: 'When filing on GitHub Issues, include the app version, reproduction steps, and any message — that speeds up diagnosis considerably. For crashes, please describe the path that led to them.' },
          { kind: 'h3', id: 'support-community', text: 'Community and discussion' },
          { kind: 'p', text: 'Feature discussion and usage questions live in GitHub Discussions; community channels such as bilibili and REDnote will open gradually with the release.' },
          { kind: 'h3', id: 'support-release', text: 'Updates and release' },
          { kind: 'p', text: '“Help > Changelog” documents the most recent packaged version, and the blog publishes progress notes and design write-ups from time to time. Release channels, licensing, and pricing are announced together with the official release.' },
        ],
      },
    ],
  },
}
export function docsCopy(language: Language) {
  return docs[language]
}
